# AIRPET Continuous Reliability Tracker

## Loop State

- Status: ACTIVE
- Current phase: continuous reliability and self-contained workflow hardening
- Active context: `docs/CONTINUOUS_RELIABILITY_CONTEXT.md`
- Cadence target: every 30 minutes

## Task Rules

1. Work the item marked `NEXT`.
2. If no item is marked `NEXT`, promote the highest-priority `PENDING` item.
3. Complete exactly one item per run.
4. Add or update tests before marking a task `DONE`.
5. Use Playwright MCP for UI-facing tasks.
6. Update the work log and choose the next `NEXT` task.
7. Do not append speculative tasks unless the tracker is nearly exhausted and
   the new task is directly supported by a reproduced workflow gap.
8. If no `NEXT` or `PENDING` tasks remain, use
   `docs/GEANT4_CAPABILITY_DISCOVERY_GUIDE.md` to run exactly one bounded
   Geant4 capability-discovery audit and seed at most three evidence-backed
   tasks.

## Status Values

- `NEXT`: work this item next
- `PENDING`: queued and ready
- `BLOCKED`: cannot proceed; explain why in notes or work log
- `DONE`: completed with tests or documented validation
- `NOOP`: no code change needed after investigation

## Current NEXT Task

| G4CAP-043 | P2 | Physics | Add `lpm` boolean to `EnvironmentState` and emit `/process/eLoss/LPM` in `generate_macro_file` | NEXT | Evidence: `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4EmParametersMessenger.cc` lines 89-94; AIRPET gap: no data model for LPM effect toggle; validation: Python macro emission test + broader regression tests |

## Backlog

| ID | Priority | Area | Task | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| ACR-002 | P0 | First Load | Add a Playwright baseline that verifies startup geometry, hierarchy, and console cleanliness | DONE | Protects the default box and hierarchy path that has regressed before |
| ACR-003 | P0 | Scoring | Add a Playwright scoring smoke that creates or loads a sensitive detector workflow, runs simulation, and verifies Analysis becomes available | DONE | Exercises GPS source, sensitive detector, scoring mesh, Geant4 run, and Analysis button enablement |
| ACR-004 | P1 | Project Load | Add a save/load/reload Playwright workflow for project JSON with sources, scoring meshes, and generated geometry | DONE | Focus on state that GDML import/export alone cannot preserve |
| ACR-005 | P1 | Detector Generators | Add a Playwright create/undo/reload workflow for tiled sensor arrays and one boolean-cut generator | DONE | Verify hierarchy, renderer, Solids panel, and stale-object cleanup |
| ACR-006 | P1 | CAD | Add a compact CAD import/reimport browser workflow using small STEP fixtures | DONE | Verify the CAD imports panel appears only when relevant and does not break narrow sidebars |
| ACR-007 | P1 | Simulation UX | Improve simulation failure/warning surfacing and add regression coverage for nonfatal artifact warnings | DONE | Backend now distinguishes warnings from errors in stderr; frontend prefixes `[WARNING]` instead of `[ERROR]` for warning lines; status stays `Completed` when Geant4 succeeds with only nonfatal artifact warnings |
| ACR-008 | P2 | Git Hygiene | Add or update local ignore guidance for Playwright logs, scratch GDML, verification output, and machine-local reference folders | DONE | `.gitignore` updated; added `tests/test_gitignore_hygiene.py` to prevent accidental removal of ignore patterns |
| ACR-009 | P2 | AI Parity | Add one route-vs-AI parity workflow for a high-value operation covered by Playwright | DONE | Added `tests/test_route_ai_parity_detector_generators.py` covering tiled_sensor_array and channel_cut_array; both paths produce equivalent geometry state after normalizing volatile IDs |
| ACR-010 | P2 | Multimodal Prep | Turn `docs/AI_MULTIMODAL_ARTIFACT_INTAKE.md` into a small spike plan with acceptance criteria and fixture needs | DONE | Created `docs/AI_MULTIMODAL_SPIKE_PLAN.md` with acceptance criteria for artifact store routes, extraction→planning→execution end-to-end, preflight cross-checks, parity reports, and Playwright coverage; enumerated fixture needs (upload files, extraction payloads, replay fixtures, UI sketch fixtures); identified test gaps (no artifact store route tests, no Playwright end-to-end). No code changes. |
| G4DISC-001 | P0 | Discovery | Run a bounded Geant4 capability-discovery audit and seed the next evidence-backed `G4CAP-*` backlog tasks | DONE | Inspected `ref/geant4-v11.3.2/examples/extended/persistency/gdml/G04`, `ref/geant4-v11.3.2/source/physics_lists/lists/src/`, and `ref/geant4-v11.3.2/examples/extended/runAndEvent/RE03`. Seeded three evidence-backed tasks. |
| G4CAP-001 | P1 | GDML | Emit and parse GDML `<auxiliary auxtype="SensDet">` tags so sensitive-detector assignments survive GDML round-trips | DONE | `src/gdml_writer.py` now emits `<auxiliary auxtype="SensDet" auxvalue=""/>` for sensitive LVs; `src/gdml_parser.py` parses it and sets `lv.is_sensitive = True`. Added `test_gdml_sensitive_detector_round_trip` in `tests/test_gdml.py`. |
| G4CAP-002 | P2 | Physics Lists | Expand the physics-list dropdown to include high-value missing lists (QBBC, QGSP_BERT_HP, QGSP_BIC, FTFP_BERT_ATL, QGSP_FTFP_BERT) | DONE | Added 5 new `<option>` values to `#simPhysicsList` in `templates/index.html`. Added `tests/test_physics_list_options.py` verifying each new list is accepted by `get_geant4_env` and forwarded to `G4PHYSICSLIST`; also guards existing lists and the omission case. |
| G4CAP-003 | P2 | Scoring | Emit `/score/create/boxMesh` and `/score/quantity/energyDeposit` macro commands for enabled scoring meshes, and instantiate `G4ScoringManager` in the C++ main | DONE | `src/project_manager.py` `generate_macro_file` now emits `/score/create/boxMesh`, `/score/mesh/boxSize`, `/score/mesh/translate/xyz`, `/score/mesh/nBin`, `/score/quantity/energyDeposit`, and `/score/close` for each enabled scoring mesh. `geant4/main.cc` now instantiates `G4ScoringManager`. Added `tests/test_scoring_macro_emission.py` with 2 tests verifying enabled mesh emission and disabled mesh omission. C++ compiles and links successfully. Follow-up: mapped tally request quantities to correct `/score/quantity/` commands (cellFlux, doseDeposit, energyDeposit, nOfStep, nOfTrack, passageCellFlux, trackLength) and fixed `generate_macro_file` to use `temp_state` instead of `self.current_geometry_state` for sensitive detectors and active sources. Added `test_generate_macro_emits_mapped_scoring_quantities_and_fallback` and `test_generate_macro_uses_version_state_not_current_state` to `tests/test_scoring_state.py`. |
| G4CAP-004 | P2 | Scoring | Add cylindrical scoring mesh support (`/score/create/cylinderMesh`) with macro emission and data model extension | DONE | Expanded `_SUPPORTED_SCORING_MESH_TYPES` to include `cylinder`; updated `_normalize_scoring_mesh_entry` to validate cylinder geometry (`rmin`, `rmax`, `z`) and bins (`r`, `phi`, `z`); updated `generate_macro_file` to emit `/score/create/cylinderMesh`, `/score/mesh/cylinderSize`, and `/score/mesh/nBin` commands; added `test_generate_macro_emits_cylinder_mesh_commands` and fixed error-message assertions in `tests/test_scoring_state.py`. All 12 scoring tests + 64 broader regression tests pass. Also fixed a syntax error (duplicate `else` block) in `generate_macro_file` from the previous G4CAP-005 commit. |
| G4CAP-005 | P2 | Scoring | Add scoring particle-filter macro support for tally requests (e.g., `/score/filter/particle gammaFilter gamma`) | DONE | Added `particle_filter` dict (`filter_name`, `particle`) to `_normalize_scoring_tally_request_entry` in `src/geometry_types.py`; fixed `generate_macro_file` in `src/project_manager.py` to emit `/score/filter/particle {filter_name} {particle}` after the quantity command; propagated `particle_filter` through `build_scoring_runtime_plan` in `src/scoring_artifacts.py`. Updated existing test and added `test_generate_macro_emits_particle_filter_for_tally_requests` in `tests/test_scoring_macro_emission.py`. All 11 scoring tests + 54 broader regression tests pass. |
| G4CAP-006 | P2 | Scoring | Expand supported tally quantities to include missing high-value Geant4 primitive scorers (nOfSecondary, passageTrackLength, flatSurfaceCurrent, flatSurfaceFlux, nOfCollision, population) | DONE | Added 6 new quantities to `_SUPPORTED_SCORING_TALLY_QUANTITIES` in `src/geometry_types.py`, to `_G4_QUANTITY_MAP` in `src/project_manager.py`, and to `SUPPORTED_SCORING_TALLY_QUANTITIES` in `static/scoringUi.js`. Added `test_generate_macro_maps_new_quantities_to_g4_commands` in `tests/test_scoring_macro_emission.py`. Also updated `tests/test_scoring_artifacts.py` and `tests/test_scoring_state.py` to reflect the expanded `supported_quantities` list. All 16 scoring tests pass. |
| G4CAP-007 | P2 | Scoring | Add `realWorldLogVol` scoring mesh support (`/score/create/realWorldLogVol`) for scoring directly on existing logical volumes | DONE | Expanded `_SUPPORTED_SCORING_MESH_TYPES` to include `realWorldLogVol`; updated `_normalize_scoring_mesh_entry` to require `geometry.logical_volume_name` and optional `geometry.copy_number_level`; updated `generate_macro_file` to emit `/score/create/realWorldLogVol {lv_name} {copy_level}` and skip mesh geometry commands. Added `test_generate_macro_emits_real_world_log_vol_commands` in `tests/test_scoring_macro_emission.py`. All 17 scoring tests pass. |
| G4CAP-008 | P2 | Scoring | Add charged, neutral, and kinetic-energy filter support for tally requests | DONE | Added `charged_filter`, `neutral_filter`, and `kinetic_energy_filter` normalization to `_normalize_scoring_tally_request_entry` in `src/geometry_types.py`; updated `generate_macro_file` in `src/project_manager.py` to emit `/score/filter/charged`, `/score/filter/neutral`, and `/score/filter/kineticEnergy` commands; propagated new filters through `build_scoring_runtime_plan` in `src/scoring_artifacts.py`; updated `normalizeScoringTally` in `static/scoringUi.js` to preserve all filter fields. Added `test_generate_macro_emits_charged_neutral_and_kinetic_energy_filters` in `tests/test_scoring_macro_emission.py`. All 18 scoring tests + 64 broader regression tests pass. |
| G4CAP-009 | P2 | Scoring | Add `probe` scoring mesh support (`/score/create/probe`) with macro emission and data model extension | DONE | Expanded `_SUPPORTED_SCORING_MESH_TYPES` to include `probe`; updated `_normalize_scoring_mesh_entry` to validate `geometry.half_size_mm`, `geometry.unit`, and `geometry.check_overlap`; updated `generate_macro_file` to emit `/score/create/probe {name} {halfSize} {unit} {checkOverlap}` and skip mesh geometry commands. Updated `normalizeScoringMesh` in `static/scoringUi.js` to preserve incoming `mesh_type` (fixing a bug where all loaded meshes were forced to `box`) and handle probe geometry. Added `test_generate_macro_emits_probe_mesh_commands` in `tests/test_scoring_macro_emission.py`. Updated mesh-type error-message assertions in `tests/test_scoring_state.py`. All 19 scoring tests pass. |
| G4CAP-010 | P2 | Scoring | Add `particleWithKineticEnergy` combined filter support (`/score/filter/particleWithKineticEnergy`) for tally requests | DONE | Added `particle_with_kinetic_energy_filter` normalization to `_normalize_scoring_tally_request_entry` in `src/geometry_types.py`; updated `generate_macro_file` in `src/project_manager.py` to emit `/score/filter/particleWithKineticEnergy`; propagated new filter through `build_scoring_runtime_plan` in `src/scoring_artifacts.py`; updated `normalizeScoringTally` in `static/scoringUi.js` to preserve the combined filter. Added `test_generate_macro_emits_particle_with_kinetic_energy_filter` in `tests/test_scoring_macro_emission.py`. All 20 scoring tests pass. |
| G4CAP-011 | P2 | Scoring | Add missing primitive scorers (`cellCharge`, `passageCellCurrent`, `volumeFlux`, `nOfTerminatedTrack`, `minKinEAtGeneration`) | DONE | Added 5 new quantities to `_SUPPORTED_SCORING_TALLY_QUANTITIES` in `src/geometry_types.py`, to `_G4_QUANTITY_MAP` in `src/project_manager.py`, and to `SUPPORTED_SCORING_TALLY_QUANTITIES` in `static/scoringUi.js`. Added `test_generate_macro_maps_additional_quantities_to_g4_commands` in `tests/test_scoring_macro_emission.py`. Updated `tests/test_scoring_artifacts.py`, `tests/test_scoring_state.py`, and `tests/test_ai_api.py` to reflect expanded `supported_quantities` list. All 22 scoring tests pass. Backlog exhausted. |
| G4CAP-012 | P1 | Materials | Parse and emit GDML `<property>` tags under `<material>` so material optical properties survive import/export round-trips | DONE | Added `properties` dict to `Material` in `src/geometry_types.py`; updated `src/gdml_parser.py` to parse `<property>` under `<material>`; updated `src/gdml_writer.py` to emit `<property>` tags; added `test_gdml_material_properties_round_trip`. All 32 GDML tests pass. |
| G4CAP-013 | P2 | Materials | Add backend API support for attaching optical properties (RINDEX, ABSLENGTH, SCINTILLATIONYIELD, etc.) to custom materials | DONE | Backend `add_material` and `update_material` already pass `properties` through kwargs after G4CAP-012. Added `tests/test_material_properties_api.py` with 4 tests: `test_add_material_with_optical_properties_survives_project_json_round_trip`, `test_update_material_optical_properties`, `test_material_properties_from_dict_restores_empty_dict`, `test_material_properties_from_dict_restores_values`. All 36 tests pass. Marked `G4CAP-014` as NEXT. |
| G4CAP-014 | P2 | Runtime | Map persisted material optical properties to `G4MaterialPropertiesTable` in the C++ detector construction so optical physics behaves correctly at runtime | DONE | Added `/g4pet/detector/addMaterialConstProperty` C++ messenger command; `DetectorConstruction::ConstructSDandField()` now populates `G4MaterialPropertiesTable` for materials with numeric const properties. `generate_macro_file` emits the command for numeric properties. Skips string refs (matrix references) in GDML writer to avoid invalid XML. Removed duplicate `test_gdml_material_properties_round_trip`. Fixed existing GDML test to include matrix defines so properties are emitted. All 72 tests pass; C++ compiles. |
| G4CAP-015 | P1 | Physics | Register `G4StepLimiterPhysics` in `geant4/main.cc` so region user limits (e.g., max step) are actually enforced by the tracking | DONE | Added `#include "G4StepLimiterPhysics.hh"` and `physicsList->RegisterPhysics(new G4StepLimiterPhysics())` in `geant4/main.cc`. Added `tests/test_step_limiter_physics.py` with `test_step_limiter_physics_is_registered` which runs a minimal batch macro and asserts `Registering G4StepLimiterPhysics` appears in stdout. C++ compiles and links; test passes in ~0.8s. |
| G4CAP-016 | P2 | Materials | Add backend support for energy-dependent material optical properties (property vectors) via GDML matrix defines and custom C++ macro command | DONE | Updated `src/gdml_parser.py` to resolve `coldim="2"` matrix refs into list-of-pairs properties. Updated `src/gdml_writer.py` to emit vector properties as synthetic matrix defines (`{mat}_{prop}_matprop_vec`) with `coldim="2"` and property refs. Updated `src/project_manager.py` `generate_macro_file` to emit `/g4pet/detector/addMaterialProperty` commands with energies converted from keV to MeV. Added C++ `SetMaterialPropertyVector` messenger command and `ConstructSDandField` loop calling `G4MaterialPropertiesTable::AddProperty`. Updated `tests/test_gdml.py` and `tests/test_material_properties_runtime.py`. All 43 focused tests pass; C++ compiles and links. |
| G4CAP-017 | P2 | Runtime | Persist optical physics enablement in project environment state and forward it to `G4OPTICALPHYSICS` env var at simulation launch | DONE | Added `optical_physics` to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate). Updated `static/scoringUi.js` `buildResolvedSimulationOptions` to fallback to `projectState.environment.optical_physics` before transient default. Updated `buildSimulationOptionOverrides` to compare against the same fallback. Removed hardcoded `optical_physics: false` from `AppState.simOptions` in `static/main.js` so loaded project state is respected. Updated `UIManager.setSimOptions` in `static/uiManager.js` to set `simPhysicsListSelect` and `simOpticalPhysicsCheckbox`. Added `tests/test_optical_physics_persistence.py` with 7 tests covering round-trip, validation, project JSON save/load, and `get_geant4_env` forwarding. Updated `tests/test_environment_state.py` for the new field. All 69 regression tests pass. |
| G4CAP-018 | P1 | Sources | Add backend data model and macro emission for ion sources (`/gun/particle ion`, `/gun/ion Z A`) | DONE | Extended `ParticleSource` in `src/geometry_types.py` with `source_type` (`gps`/`ion`) and `ion_params` dict (`Z`, `A`, `Q`, `excitation_energy_keV`). Updated `to_dict`/`from_dict` for round-trip persistence. Updated `add_particle_source` and `add_source` in `src/project_manager.py` to accept and forward the new fields. Updated `generate_macro_file` to emit `/gps/particle ion` and `/gps/ion {Z} {A} {Q} {E}` for ion sources, skipping duplicate `particle`/`ion` gps_commands keys. Added `tests/test_ion_source_macro.py` with 4 tests: ion command emission, spurious-gps-command skipping, GPS source regression, and dict round-trip. All 134 focused tests pass. |
| G4CAP-019 | P1 | Physics | Register `G4RadioactiveDecayPhysics` in `geant4/main.cc` conditional on an environment variable so decay chains are simulated | DONE | Added `#include "G4RadioactiveDecayPhysics.hh"` and conditional registration in `geant4/main.cc` based on `G4RADIOACTIVEDECAYPHYSICS` env var. Updated `app.py` `get_geant4_env` to forward `radioactive_decay_physics` from `sim_params`. Added `tests/test_radioactive_decay_physics.py` with 5 tests: registration when env var set, no registration when absent, and `get_geant4_env` true/false/omission coverage. C++ compiles and links; all 5 new tests + 33 broader regression tests pass. |
| G4CAP-020 | P2 | Sources | Add UI controls and AI tool normalization for ion source parameters (Z, A, Q, excitation energy) | DONE | Added source type selector and ion param inputs to GPS editor modal; updated backend routes, project manager, and AI tool normalization to persist and forward `source_type` and `ion_params`. Fixed pre-existing G4CAP-017 `main.js` corruption that broke module loading. Added Playwright UI test `tests/test_ion_source_ui.py` and AI tool test `tests/test_ai_api.py::test_ai_tool_manage_particle_source_ion_params`. All 25 focused tests pass. |
| G4CAP-021 | P1 | Optical Surfaces | Add GDML round-trip tests for optical surfaces, skin surfaces, and border surfaces, fixing any parser/writer issues found | DONE | Fixed `test_gdml_border_surface_round_trip` assertion: after GDML round-trip, PhysicalVolumePlacement objects are recreated with new UUIDs, so verifying by original ID is impossible. Test now asserts that imported border-surface PV refs resolve to PVs named `box_pv`. All 35 GDML tests pass. |
| G4CAP-022 | P1 | Optical Surfaces | Add C++ runtime support and macro emission for optical surfaces, skin surfaces, and border surfaces | DONE | Added `/g4pet/detector/addOpticalSurface`, `addSkinSurface`, `addBorderSurface` messenger commands to `geant4/src/DetectorConstruction.cc`; updated `generate_macro_file` in `src/project_manager.py` to emit them; added enum parsing helpers for `G4SurfaceType`, `G4OpticalSurfaceModel`, and `G4OpticalSurfaceFinish`. Added `tests/test_optical_surface_macro.py` (2 tests) and `tests/test_optical_surface_cpp.py` (1 test). C++ compiles and links; all 3 new tests + 27 focused regression tests pass. Backlog exhausted. |
| G4CAP-023 | P1 | Optical Surfaces | Add C++ runtime support for optical surface properties (const and vector) via messenger commands and macro emission | DONE | Added `/g4pet/detector/addOpticalSurfacePropertyConst` and `/g4pet/detector/addOpticalSurfaceProperty` messenger commands to `geant4/src/DetectorConstruction.cc`; updated `ConstructSDandField` to create `G4MaterialPropertiesTable` for each optical surface and call `opSurf->SetMaterialPropertiesTable`. Updated `generate_macro_file` in `src/project_manager.py` to emit const/vector property commands for optical surfaces, including matrix-define resolution. Updated `src/gdml_writer.py` to collect synthetic defines for optical surface list properties and emit them as matrix refs. Updated `tests/test_gdml.py::test_gdml_optical_surface_round_trip` to include a matrix define so the property is emitted. Added 3 new macro tests (`test_generate_macro_emits_optical_surface_const_property`, `test_generate_macro_emits_optical_surface_vector_property`, `test_generate_macro_resolves_optical_surface_matrix_define`) and 1 C++ runtime test (`test_optical_surface_properties_are_attached_at_runtime`). C++ compiles and links; all 67 focused tests pass. Backlog exhausted. |
| G4CAP-024 | P1 | Optical Physics | Persist `optical_boundary_invoke_sd` in project environment state and emit `/process/optical/boundary/setInvokeSD` when optical physics is enabled | DONE | Added `optical_boundary_invoke_sd` to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/boundary/setInvokeSD true/false` after `/run/initialize` when optical physics is enabled. Added `tests/test_optical_boundary_invoke_sd.py` with 6 tests covering round-trip, validation, project JSON persistence, true emission, false emission, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 28 focused tests pass; 773 broader regression tests pass (4 pre-existing unrelated failures). |
| G4CAP-025 | P1 | Physics | Add process inactivation support (`/process/inactivate`) to `EnvironmentState` and `generate_macro_file` | DONE | Added `process_inactivation` list to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, normalization). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/inactivate` commands after `/run/initialize`. Added `tests/test_process_inactivation.py` (5 tests). Updated existing test dict assertions for the new field. All 5 new tests + 28 focused tests pass; 774 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-026` as NEXT. |
| G4CAP-026 | P2 | Physics | Add EM process parameter support (`/process/em/applyCuts` and `/process/eLoss/fluct`) to `EnvironmentState` and `generate_macro_file` | DONE | Added `em_apply_cuts` (default `False`) and `eloss_fluct` (default `True`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/applyCuts true` when `em_apply_cuts` is `True` and `/process/eLoss/fluct false` when `eloss_fluct` is `False`, after `/run/initialize`. Added `tests/test_em_process_parameters.py` with 7 tests covering round-trip, validation, project JSON persistence, emission for each field individually, combined emission, and omission at defaults. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 7 new tests + 28 focused tests pass; 781 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-027` as NEXT. |
| G4CAP-027 | P2 | Fields | Add field propagation parameter support (`/field/stepperType` and `/field/setMinimumStep`) to `EnvironmentState` and `generate_macro_file` | DONE | Added `field_stepper_type` (default `""`) and `field_minimum_step_mm` (default `0.0`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/field/stepperType {type}` when non-empty and `/field/setMinimumStep {value} mm` when greater than zero, after field assignments and before region controls. Added `tests/test_field_propagation_parameters.py` with 7 tests covering round-trip, validation, project JSON persistence, stepper-only emission, minimum-step-only emission, combined emission, and omission at defaults. Updated `tests/test_environment_state.py`, `tests/test_geant4_field_macro.py`, `tests/test_ai_api.py`, and `tests/test_field_aware_examples.py` for the new fields in expected dicts. All 7 new tests + 28 focused tests pass; 788 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-028` as NEXT. |
| G4CAP-028 | P2 | Optical Physics | Add `cerenkov_max_photons` to `EnvironmentState` and emit `/process/optical/cerenkov/setMaxPhotons` when optical physics is enabled | DONE | Added `cerenkov_max_photons` (default `0`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/cerenkov/setMaxPhotons {value}` after `/run/initialize` when optical physics is enabled and value is greater than zero. Added `tests/test_cerenkov_max_photons.py` with 6 tests covering round-trip, validation, project JSON persistence, positive emission, default omission, and optical-physics-off omission. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 6 new tests + 795 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-029` as NEXT. |
| G4CAP-029 | P2 | Optical Physics | Add `scintillation_by_particle_type` and `scintillation_finite_rise_time` to `EnvironmentState` and emit corresponding `/process/optical/scintillation/...` commands | DONE | Added `scintillation_by_particle_type` (default `False`) and `scintillation_finite_rise_time` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/scintillation/setByParticleType true` and `/process/optical/scintillation/setFiniteRiseTime true` after `/run/initialize` when optical physics is enabled and each flag is true. Added `tests/test_scintillation_process_controls.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 8 new tests + 191 broader regression tests pass. Marked `G4CAP-030` as NEXT. |
| G4CAP-030 | P2 | Run Control | Add `tracking_verbose` and `hits_verbose` to run manifest and emit `/tracking/verbose` and `/hits/verbose` before `/run/beamOn` | DONE | Added `tracking_verbose` (default `0`) and `hits_verbose` (default `0`) to `_default_scoring_run_manifest_defaults()` and `_normalize_scoring_run_manifest_defaults()` in `src/geometry_types.py`. Updated `generate_macro_file` in `src/project_manager.py` to emit `/tracking/verbose {value}` and `/hits/verbose {value}` before `/run/beamOn` when each value is greater than zero. Updated `static/scoringUi.js` to include the new fields in `SCORING_SAVED_RUN_CONTROL_KEYS`, `DEFAULT_RUN_MANIFEST_DEFAULTS`, and `normalizeRunManifestDefaults`. Added `tests/test_tracking_hits_verbose.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and ordering before `/run/beamOn`. Updated `tests/test_scoring_state.py` and `tests/js/scoring_ui.test.mjs` expected dicts for the new fields. All 8 new tests + 69 broader regression tests pass; JS tests pass. Backlog exhausted. |
| G4CAP-031 | P1 | Physics | Add `fluo` (atomic deexcitation) and `auger` boolean flags to `EnvironmentState` and emit `/process/em/fluo` and `/process/em/auger` in `generate_macro_file` | DONE | Added `fluo` (default `False`) and `auger` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/fluo true` and `/process/em/auger true` after `/run/initialize` when each flag is true. Added `tests/test_fluo_auger.py` with 7 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, and omission at defaults. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 7 new tests + 28 focused tests pass; 175 broader regression tests pass. Marked `G4CAP-032` as NEXT. |
| G4CAP-032 | P2 | Physics | Add `auger_cascade`, `pixe`, and `deexcitation_ignore_cut` boolean flags to `EnvironmentState` and emit corresponding `/process/em/...` commands in `generate_macro_file` | DONE | Added `auger_cascade`, `pixe`, and `deexcitation_ignore_cut` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/augerCascade true`, `/process/em/pixe true`, and `/process/em/deexcitationIgnoreCut true` after `/run/initialize` when each flag is true. Added `tests/test_auger_cascade_pixe_deexcitation.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, and omission at defaults. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 8 new tests + 28 focused tests pass; 826 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-033` as NEXT. |
| G4CAP-033 | P2 | Physics | Add `em_integral` boolean flag to `EnvironmentState` and emit `/process/em/integral` in `generate_macro_file` | DONE | Added `em_integral` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/integral true` after `/run/initialize` when the flag is true. Added `tests/test_em_integral.py` with 5 tests covering round-trip, validation, project JSON persistence, emission when true, and omission at default. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 22 focused tests pass; 197 broader regression tests pass. Backlog exhausted; performed discovery audit. |
| G4CAP-034 | P2 | Optical Physics | Add `cerenkov_track_secondaries_first` and `scintillation_track_secondaries_first` to `EnvironmentState` and emit corresponding `/process/optical/...` commands | DONE | Added `cerenkov_track_secondaries_first` (default `False`) and `scintillation_track_secondaries_first` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/cerenkov/setTrackSecondariesFirst true` and `/process/optical/scintillation/setTrackSecondariesFirst true` after `/run/initialize` when optical physics is enabled and each flag is true. Added `tests/test_cerenkov_scintillation_track_secondaries_first.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 8 new tests + 28 focused tests pass; 252 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-035` as NEXT. |
| G4CAP-035 | P2 | Physics | Add `em_use_saturation` (Birks) boolean to `EnvironmentState` and emit `/process/em/UseG4EmSaturation` | DONE | Added `em_use_saturation` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/UseG4EmSaturation true` after `/run/initialize` when the flag is true. Added `tests/test_em_use_saturation.py` with 5 tests covering round-trip, validation, project JSON persistence, emission when true, and omission at default. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 28 focused tests pass; 844 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-036` as NEXT. |
| G4CAP-036 | P2 | Physics | Add `em_polarisation` boolean to `EnvironmentState` and emit `/process/em/Polarisation` | DONE | Added `em_polarisation` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/Polarisation true` after `/run/initialize` when the flag is true. Added `tests/test_em_polarisation.py` with 5 tests covering round-trip, validation, project JSON persistence, emission when true, and omission at default. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 177 broader regression tests pass (4 pre-existing unrelated failures). Backlog exhausted. |
| G4CAP-037 | P2 | Optical Physics | Add `cerenkov_stack_photons` and `scintillation_stack_photons` to `EnvironmentState` and emit `/process/optical/cerenkov/setStackPhotons` and `/process/optical/scintillation/setStackPhotons` | DONE | Added `cerenkov_stack_photons` (default `True`) and `scintillation_stack_photons` (default `True`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/cerenkov/setStackPhotons false` and `/process/optical/scintillation/setStackPhotons false` after `/run/initialize` when optical physics is enabled and each flag is `False`. Added `tests/test_optical_stack_photons.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 8 new tests + 857 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-038` as NEXT. |
| G4CAP-038 | P2 | Run Control | Add `run_verbose` and `event_verbose` to run manifest defaults and emit `/run/verbose` and `/event/verbose` before `/run/beamOn` | DONE | Added `run_verbose` (default `0`) and `event_verbose` (default `0`) to `_default_scoring_run_manifest_defaults()` and `_normalize_scoring_run_manifest_defaults()` in `src/geometry_types.py`. Updated `generate_macro_file` in `src/project_manager.py` to emit `/run/verbose {value}` and `/event/verbose {value}` before `/run/beamOn` when each value is greater than zero. Updated `static/scoringUi.js` to include the new fields in `SCORING_SAVED_RUN_CONTROL_KEYS`, `DEFAULT_RUN_MANIFEST_DEFAULTS`, and `normalizeRunManifestDefaults`. Added `tests/test_run_event_verbose.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and ordering before `/run/beamOn`. Updated `tests/test_scoring_state.py` and `tests/js/scoring_ui.test.mjs` expected dicts for the new fields. All 8 new tests + 191 broader regression tests pass. Marked `G4CAP-039` as NEXT. |
| G4CAP-039 | P2 | Physics | Add `em_verbose` to `EnvironmentState` and emit `/process/em/verbose` in `generate_macro_file` | DONE | Added `em_verbose` (default `0`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/verbose {value}` after `/run/initialize` when value is greater than zero. Added `tests/test_em_verbose.py` with 5 tests covering round-trip, validation, project JSON persistence, emission when positive, and omission at default. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 9 focused tests pass; 212 broader regression tests pass. Backlog exhausted. |
| G4CAP-040 | P2 | Optical Physics | Add `cerenkov_max_beta_change` to `EnvironmentState` and emit `/process/optical/cerenkov/setMaxBetaChange` in `generate_macro_file` | DONE | Added `cerenkov_max_beta_change` (default `0.0`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/cerenkov/setMaxBetaChange {value}` after `/run/initialize` when optical physics is enabled and value is greater than zero. Added `tests/test_cerenkov_max_beta_change.py` with 6 tests covering round-trip, validation, project JSON persistence, emission when positive, omission at default, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 6 new tests + 124 broader regression tests pass. Marked `G4CAP-041` as NEXT. |
| G4CAP-041 | P2 | Optical Physics | Add `scintillation_track_info` to `EnvironmentState` and emit `/process/optical/scintillation/setTrackInfo` in `generate_macro_file` | DONE | Added `scintillation_track_info` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/scintillation/setTrackInfo true` after `/run/initialize` when optical physics is enabled and the flag is true. Added `tests/test_scintillation_track_info.py` with 6 tests covering round-trip, validation, project JSON persistence, emission when true, omission at default, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 6 new tests + 183 focused tests pass; 882 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-042` as NEXT. |
| G4CAP-042 | P2 | Optical Physics | Add `wls_time_profile` and `wls2_time_profile` to `EnvironmentState` and emit `/process/optical/wls/setTimeProfile` and `/process/optical/wls2/setTimeProfile` in `generate_macro_file` | DONE | Added `wls_time_profile` (default `""`) and `wls2_time_profile` (default `""`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/wls/setTimeProfile {value}` and `/process/optical/wls2/setTimeProfile {value}` after `/run/initialize` when optical physics is enabled and each value is non-empty. Added `tests/test_wls_time_profile.py` with 9 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, omission when optical physics is off, and summary dict behavior. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 9 new tests + 302 broader regression tests pass. Backlog exhausted. |
| G4CAP-043 | P2 | Physics | Add `lpm` boolean to `EnvironmentState` and emit `/process/eLoss/LPM` in `generate_macro_file` | NEXT | Evidence: `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4EmParametersMessenger.cc` lines 89-94; AIRPET gap: no data model for LPM effect toggle; validation: Python macro emission test + broader regression tests |
| G4CAP-044 | P2 | Physics | Add `msc_lateral_displacement` and `msc_mu_had_lateral_displacement` booleans to `EnvironmentState` and emit `/process/msc/LateralDisplacement` and `/process/msc/MuHadLateralDisplacement` in `generate_macro_file` | PENDING | Evidence: `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4EmParametersMessenger.cc` lines 117-135; AIRPET gap: no data model for MSC lateral displacement controls; validation: Python macro emission test + broader regression tests |
| G4CAP-045 | P2 | Physics | Add `msc_step_limit` and `msc_step_limit_mu_had` strings to `EnvironmentState` and emit `/process/msc/StepLimit` and `/process/msc/StepLimitMuHad` in `generate_macro_file` | PENDING | Evidence: `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4EmParametersMessenger.cc` lines 415-426; AIRPET gap: no data model for MSC step limitation type; validation: Python macro emission test + broader regression tests |

## Backlog

| ID | Priority | Area | Task | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| ACR-002 | P0 | First Load | Add a Playwright baseline that verifies startup geometry, hierarchy, and console cleanliness | DONE | Protects the default box and hierarchy path that has regressed before |
| ACR-003 | P0 | Scoring | Add a Playwright scoring smoke that creates or loads a sensitive detector workflow, runs simulation, and verifies Analysis becomes available | DONE | Exercises GPS source, sensitive detector, scoring mesh, Geant4 run, and Analysis button enablement |
| ACR-004 | P1 | Project Load | Add a save/load/reload Playwright workflow for project JSON with sources, scoring meshes, and generated geometry | DONE | Focus on state that GDML import/export alone cannot preserve |
| ACR-005 | P1 | Detector Generators | Add a Playwright create/undo/reload workflow for tiled sensor arrays and one boolean-cut generator | DONE | Verify hierarchy, renderer, Solids panel, and stale-object cleanup |
| ACR-006 | P1 | CAD | Add a compact CAD import/reimport browser workflow using small STEP fixtures | DONE | Verify the CAD imports panel appears only when relevant and does not break narrow sidebars |
| ACR-007 | P1 | Simulation UX | Improve simulation failure/warning surfacing and add regression coverage for nonfatal artifact warnings | DONE | Backend now distinguishes warnings from errors in stderr; frontend prefixes `[WARNING]` instead of `[ERROR]` for warning lines; status stays `Completed` when Geant4 succeeds with only nonfatal artifact warnings |
| ACR-008 | P2 | Git Hygiene | Add or update local ignore guidance for Playwright logs, scratch GDML, verification output, and machine-local reference folders | DONE | `.gitignore` updated; added `tests/test_gitignore_hygiene.py` to prevent accidental removal of ignore patterns |
| ACR-009 | P2 | AI Parity | Add one route-vs-AI parity workflow for a high-value operation covered by Playwright | DONE | Added `tests/test_route_ai_parity_detector_generators.py` covering tiled_sensor_array and channel_cut_array; both paths produce equivalent geometry state after normalizing volatile IDs |
| ACR-010 | P2 | Multimodal Prep | Turn `docs/AI_MULTIMODAL_ARTIFACT_INTAKE.md` into a small spike plan with acceptance criteria and fixture needs | DONE | Created `docs/AI_MULTIMODAL_SPIKE_PLAN.md` with acceptance criteria for artifact store routes, extraction→planning→execution end-to-end, preflight cross-checks, parity reports, and Playwright coverage; enumerated fixture needs (upload files, extraction payloads, replay fixtures, UI sketch fixtures); identified test gaps (no artifact store route tests, no Playwright end-to-end). No code changes. |
| G4DISC-001 | P0 | Discovery | Run a bounded Geant4 capability-discovery audit and seed the next evidence-backed `G4CAP-*` backlog tasks | DONE | Inspected `ref/geant4-v11.3.2/examples/extended/persistency/gdml/G04`, `ref/geant4-v11.3.2/source/physics_lists/lists/src/`, and `ref/geant4-v11.3.2/examples/extended/runAndEvent/RE03`. Seeded three evidence-backed tasks. |
| G4CAP-001 | P1 | GDML | Emit and parse GDML `<auxiliary auxtype="SensDet">` tags so sensitive-detector assignments survive GDML round-trips | DONE | `src/gdml_writer.py` now emits `<auxiliary auxtype="SensDet" auxvalue=""/>` for sensitive LVs; `src/gdml_parser.py` parses it and sets `lv.is_sensitive = True`. Added `test_gdml_sensitive_detector_round_trip` in `tests/test_gdml.py`. |
| G4CAP-002 | P2 | Physics Lists | Expand the physics-list dropdown to include high-value missing lists (QBBC, QGSP_BERT_HP, QGSP_BIC, FTFP_BERT_ATL, QGSP_FTFP_BERT) | DONE | Added 5 new `<option>` values to `#simPhysicsList` in `templates/index.html`. Added `tests/test_physics_list_options.py` verifying each new list is accepted by `get_geant4_env` and forwarded to `G4PHYSICSLIST`; also guards existing lists and the omission case. |
| G4CAP-003 | P2 | Scoring | Emit `/score/create/boxMesh` and `/score/quantity/energyDeposit` macro commands for enabled scoring meshes, and instantiate `G4ScoringManager` in the C++ main | DONE | `src/project_manager.py` `generate_macro_file` now emits `/score/create/boxMesh`, `/score/mesh/boxSize`, `/score/mesh/translate/xyz`, `/score/mesh/nBin`, `/score/quantity/energyDeposit`, and `/score/close` for each enabled scoring mesh. `geant4/main.cc` now instantiates `G4ScoringManager`. Added `tests/test_scoring_macro_emission.py` with 2 tests verifying enabled mesh emission and disabled mesh omission. C++ compiles and links successfully. Follow-up: mapped tally request quantities to correct `/score/quantity/` commands (cellFlux, doseDeposit, energyDeposit, nOfStep, nOfTrack, passageCellFlux, trackLength) and fixed `generate_macro_file` to use `temp_state` instead of `self.current_geometry_state` for sensitive detectors and active sources. Added `test_generate_macro_emits_mapped_scoring_quantities_and_fallback` and `test_generate_macro_uses_version_state_not_current_state` to `tests/test_scoring_state.py`. |
| G4CAP-004 | P2 | Scoring | Add cylindrical scoring mesh support (`/score/create/cylinderMesh`) with macro emission and data model extension | DONE | Expanded `_SUPPORTED_SCORING_MESH_TYPES` to include `cylinder`; updated `_normalize_scoring_mesh_entry` to validate cylinder geometry (`rmin`, `rmax`, `z`) and bins (`r`, `phi`, `z`); updated `generate_macro_file` to emit `/score/create/cylinderMesh`, `/score/mesh/cylinderSize`, and `/score/mesh/nBin` commands; added `test_generate_macro_emits_cylinder_mesh_commands` and fixed error-message assertions in `tests/test_scoring_state.py`. All 12 scoring tests + 64 broader regression tests pass. Also fixed a syntax error (duplicate `else` block) in `generate_macro_file` from the previous G4CAP-005 commit. |
| G4CAP-005 | P2 | Scoring | Add scoring particle-filter macro support for tally requests (e.g., `/score/filter/particle gammaFilter gamma`) | DONE | Added `particle_filter` dict (`filter_name`, `particle`) to `_normalize_scoring_tally_request_entry` in `src/geometry_types.py`; fixed `generate_macro_file` in `src/project_manager.py` to emit `/score/filter/particle {filter_name} {particle}` after the quantity command; propagated `particle_filter` through `build_scoring_runtime_plan` in `src/scoring_artifacts.py`. Updated existing test and added `test_generate_macro_emits_particle_filter_for_tally_requests` in `tests/test_scoring_macro_emission.py`. All 11 scoring tests + 54 broader regression tests pass. |
| G4CAP-006 | P2 | Scoring | Expand supported tally quantities to include missing high-value Geant4 primitive scorers (nOfSecondary, passageTrackLength, flatSurfaceCurrent, flatSurfaceFlux, nOfCollision, population) | DONE | Added 6 new quantities to `_SUPPORTED_SCORING_TALLY_QUANTITIES` in `src/geometry_types.py`, to `_G4_QUANTITY_MAP` in `src/project_manager.py`, and to `SUPPORTED_SCORING_TALLY_QUANTITIES` in `static/scoringUi.js`. Added `test_generate_macro_maps_new_quantities_to_g4_commands` in `tests/test_scoring_macro_emission.py`. Also updated `tests/test_scoring_artifacts.py` and `tests/test_scoring_state.py` to reflect the expanded `supported_quantities` list. All 16 scoring tests pass. |
| G4CAP-007 | P2 | Scoring | Add `realWorldLogVol` scoring mesh support (`/score/create/realWorldLogVol`) for scoring directly on existing logical volumes | DONE | Expanded `_SUPPORTED_SCORING_MESH_TYPES` to include `realWorldLogVol`; updated `_normalize_scoring_mesh_entry` to require `geometry.logical_volume_name` and optional `geometry.copy_number_level`; updated `generate_macro_file` to emit `/score/create/realWorldLogVol {lv_name} {copy_level}` and skip mesh geometry commands. Added `test_generate_macro_emits_real_world_log_vol_commands` in `tests/test_scoring_macro_emission.py`. All 17 scoring tests pass. |
| G4CAP-008 | P2 | Scoring | Add charged, neutral, and kinetic-energy filter support for tally requests | DONE | Added `charged_filter`, `neutral_filter`, and `kinetic_energy_filter` normalization to `_normalize_scoring_tally_request_entry` in `src/geometry_types.py`; updated `generate_macro_file` in `src/project_manager.py` to emit `/score/filter/charged`, `/score/filter/neutral`, and `/score/filter/kineticEnergy` commands; propagated new filters through `build_scoring_runtime_plan` in `src/scoring_artifacts.py`; updated `normalizeScoringTally` in `static/scoringUi.js` to preserve all filter fields. Added `test_generate_macro_emits_charged_neutral_and_kinetic_energy_filters` in `tests/test_scoring_macro_emission.py`. All 18 scoring tests + 64 broader regression tests pass. |
| G4CAP-009 | P2 | Scoring | Add `probe` scoring mesh support (`/score/create/probe`) with macro emission and data model extension | DONE | Expanded `_SUPPORTED_SCORING_MESH_TYPES` to include `probe`; updated `_normalize_scoring_mesh_entry` to validate `geometry.half_size_mm`, `geometry.unit`, and `geometry.check_overlap`; updated `generate_macro_file` to emit `/score/create/probe {name} {halfSize} {unit} {checkOverlap}` and skip mesh geometry commands. Updated `normalizeScoringMesh` in `static/scoringUi.js` to preserve incoming `mesh_type` (fixing a bug where all loaded meshes were forced to `box`) and handle probe geometry. Added `test_generate_macro_emits_probe_mesh_commands` in `tests/test_scoring_macro_emission.py`. Updated mesh-type error-message assertions in `tests/test_scoring_state.py`. All 19 scoring tests pass. |
| G4CAP-010 | P2 | Scoring | Add `particleWithKineticEnergy` combined filter support (`/score/filter/particleWithKineticEnergy`) for tally requests | DONE | Added `particle_with_kinetic_energy_filter` normalization to `_normalize_scoring_tally_request_entry` in `src/geometry_types.py`; updated `generate_macro_file` in `src/project_manager.py` to emit `/score/filter/particleWithKineticEnergy`; propagated new filter through `build_scoring_runtime_plan` in `src/scoring_artifacts.py`; updated `normalizeScoringTally` in `static/scoringUi.js` to preserve the combined filter. Added `test_generate_macro_emits_particle_with_kinetic_energy_filter` in `tests/test_scoring_macro_emission.py`. All 20 scoring tests pass. |
| G4CAP-011 | P2 | Scoring | Add missing primitive scorers (`cellCharge`, `passageCellCurrent`, `volumeFlux`, `nOfTerminatedTrack`, `minKinEAtGeneration`) | DONE | Added 5 new quantities to `_SUPPORTED_SCORING_TALLY_QUANTITIES` in `src/geometry_types.py`, to `_G4_QUANTITY_MAP` in `src/project_manager.py`, and to `SUPPORTED_SCORING_TALLY_QUANTITIES` in `static/scoringUi.js`. Added `test_generate_macro_maps_additional_quantities_to_g4_commands` in `tests/test_scoring_macro_emission.py`. Updated `tests/test_scoring_artifacts.py`, `tests/test_scoring_state.py`, and `tests/test_ai_api.py` to reflect expanded `supported_quantities` list. All 22 scoring tests pass. Backlog exhausted. |
| G4CAP-012 | P1 | Materials | Parse and emit GDML `<property>` tags under `<material>` so material optical properties survive import/export round-trips | DONE | Added `properties` dict to `Material` in `src/geometry_types.py`; updated `src/gdml_parser.py` to parse `<property>` under `<material>`; updated `src/gdml_writer.py` to emit `<property>` tags; added `test_gdml_material_properties_round_trip`. All 32 GDML tests pass. |
| G4CAP-013 | P2 | Materials | Add backend API support for attaching optical properties (RINDEX, ABSLENGTH, SCINTILLATIONYIELD, etc.) to custom materials | DONE | Backend `add_material` and `update_material` already pass `properties` through kwargs after G4CAP-012. Added `tests/test_material_properties_api.py` with 4 tests: `test_add_material_with_optical_properties_survives_project_json_round_trip`, `test_update_material_optical_properties`, `test_material_properties_from_dict_restores_empty_dict`, `test_material_properties_from_dict_restores_values`. All 36 tests pass. Marked `G4CAP-014` as NEXT. |
| G4CAP-014 | P2 | Runtime | Map persisted material optical properties to `G4MaterialPropertiesTable` in the C++ detector construction so optical physics behaves correctly at runtime | DONE | Added `/g4pet/detector/addMaterialConstProperty` C++ messenger command; `DetectorConstruction::ConstructSDandField()` now populates `G4MaterialPropertiesTable` for materials with numeric const properties. `generate_macro_file` emits the command for numeric properties. Skips string refs (matrix references) in GDML writer to avoid invalid XML. Removed duplicate `test_gdml_material_properties_round_trip`. Fixed existing GDML test to include matrix defines so properties are emitted. All 72 tests pass; C++ compiles. |
| G4CAP-015 | P1 | Physics | Register `G4StepLimiterPhysics` in `geant4/main.cc` so region user limits (e.g., max step) are actually enforced by the tracking | DONE | Added `#include "G4StepLimiterPhysics.hh"` and `physicsList->RegisterPhysics(new G4StepLimiterPhysics())` in `geant4/main.cc`. Added `tests/test_step_limiter_physics.py` with `test_step_limiter_physics_is_registered` which runs a minimal batch macro and asserts `Registering G4StepLimiterPhysics` appears in stdout. C++ compiles and links; test passes in ~0.8s. |
| G4CAP-016 | P2 | Materials | Add backend support for energy-dependent material optical properties (property vectors) via GDML matrix defines and custom C++ macro command | DONE | Updated `src/gdml_parser.py` to resolve `coldim="2"` matrix refs into list-of-pairs properties. Updated `src/gdml_writer.py` to emit vector properties as synthetic matrix defines (`{mat}_{prop}_matprop_vec`) with `coldim="2"` and property refs. Updated `src/project_manager.py` `generate_macro_file` to emit `/g4pet/detector/addMaterialProperty` commands with energies converted from keV to MeV. Added C++ `SetMaterialPropertyVector` messenger command and `ConstructSDandField` loop calling `G4MaterialPropertiesTable::AddProperty`. Updated `tests/test_gdml.py` and `tests/test_material_properties_runtime.py`. All 43 focused tests pass; C++ compiles and links. |
| G4CAP-017 | P2 | Runtime | Persist optical physics enablement in project environment state and forward it to `G4OPTICALPHYSICS` env var at simulation launch | DONE | Added `optical_physics` to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate). Updated `static/scoringUi.js` `buildResolvedSimulationOptions` to fallback to `projectState.environment.optical_physics` before transient default. Updated `buildSimulationOptionOverrides` to compare against the same fallback. Removed hardcoded `optical_physics: false` from `AppState.simOptions` in `static/main.js` so loaded project state is respected. Updated `UIManager.setSimOptions` in `static/uiManager.js` to set `simPhysicsListSelect` and `simOpticalPhysicsCheckbox`. Added `tests/test_optical_physics_persistence.py` with 7 tests covering round-trip, validation, project JSON save/load, and `get_geant4_env` forwarding. Updated `tests/test_environment_state.py` for the new field. All 69 regression tests pass. |
| G4CAP-018 | P1 | Sources | Add backend data model and macro emission for ion sources (`/gun/particle ion`, `/gun/ion Z A`) | DONE | Extended `ParticleSource` in `src/geometry_types.py` with `source_type` (`gps`/`ion`) and `ion_params` dict (`Z`, `A`, `Q`, `excitation_energy_keV`). Updated `to_dict`/`from_dict` for round-trip persistence. Updated `add_particle_source` and `add_source` in `src/project_manager.py` to accept and forward the new fields. Updated `generate_macro_file` to emit `/gps/particle ion` and `/gps/ion {Z} {A} {Q} {E}` for ion sources, skipping duplicate `particle`/`ion` gps_commands keys. Added `tests/test_ion_source_macro.py` with 4 tests: ion command emission, spurious-gps-command skipping, GPS source regression, and dict round-trip. All 134 focused tests pass. |
| G4CAP-019 | P1 | Physics | Register `G4RadioactiveDecayPhysics` in `geant4/main.cc` conditional on an environment variable so decay chains are simulated | DONE | Added `#include "G4RadioactiveDecayPhysics.hh"` and conditional registration in `geant4/main.cc` based on `G4RADIOACTIVEDECAYPHYSICS` env var. Updated `app.py` `get_geant4_env` to forward `radioactive_decay_physics` from `sim_params`. Added `tests/test_radioactive_decay_physics.py` with 5 tests: registration when env var set, no registration when absent, and `get_geant4_env` true/false/omission coverage. C++ compiles and links; all 5 new tests + 33 broader regression tests pass. |
| G4CAP-020 | P2 | Sources | Add UI controls and AI tool normalization for ion source parameters (Z, A, Q, excitation energy) | DONE | Added source type selector and ion param inputs to GPS editor modal; updated backend routes, project manager, and AI tool normalization to persist and forward `source_type` and `ion_params`. Fixed pre-existing G4CAP-017 `main.js` corruption that broke module loading. Added Playwright UI test `tests/test_ion_source_ui.py` and AI tool test `tests/test_ai_api.py::test_ai_tool_manage_particle_source_ion_params`. All 25 focused tests pass. |
| G4CAP-021 | P1 | Optical Surfaces | Add GDML round-trip tests for optical surfaces, skin surfaces, and border surfaces, fixing any parser/writer issues found | DONE | Fixed `test_gdml_border_surface_round_trip` assertion: after GDML round-trip, PhysicalVolumePlacement objects are recreated with new UUIDs, so verifying by original ID is impossible. Test now asserts that imported border-surface PV refs resolve to PVs named `box_pv`. All 35 GDML tests pass. |
| G4CAP-022 | P1 | Optical Surfaces | Add C++ runtime support and macro emission for optical surfaces, skin surfaces, and border surfaces | DONE | Added `/g4pet/detector/addOpticalSurface`, `addSkinSurface`, `addBorderSurface` messenger commands to `geant4/src/DetectorConstruction.cc`; updated `generate_macro_file` in `src/project_manager.py` to emit them; added enum parsing helpers for `G4SurfaceType`, `G4OpticalSurfaceModel`, and `G4OpticalSurfaceFinish`. Added `tests/test_optical_surface_macro.py` (2 tests) and `tests/test_optical_surface_cpp.py` (1 test). C++ compiles and links; all 3 new tests + 27 focused regression tests pass. Backlog exhausted. |
| G4CAP-023 | P1 | Optical Surfaces | Add C++ runtime support for optical surface properties (const and vector) via messenger commands and macro emission | DONE | Added `/g4pet/detector/addOpticalSurfacePropertyConst` and `/g4pet/detector/addOpticalSurfaceProperty` messenger commands to `geant4/src/DetectorConstruction.cc`; updated `ConstructSDandField` to create `G4MaterialPropertiesTable` for each optical surface and call `opSurf->SetMaterialPropertiesTable`. Updated `generate_macro_file` in `src/project_manager.py` to emit const/vector property commands for optical surfaces, including matrix-define resolution. Updated `src/gdml_writer.py` to collect synthetic defines for optical surface list properties and emit them as matrix refs. Updated `tests/test_gdml.py::test_gdml_optical_surface_round_trip` to include a matrix define so the property is emitted. Added 3 new macro tests (`test_generate_macro_emits_optical_surface_const_property`, `test_generate_macro_emits_optical_surface_vector_property`, `test_generate_macro_resolves_optical_surface_matrix_define`) and 1 C++ runtime test (`test_optical_surface_properties_are_attached_at_runtime`). C++ compiles and links; all 67 focused tests pass. Backlog exhausted. |
| G4CAP-024 | P1 | Optical Physics | Persist `optical_boundary_invoke_sd` in project environment state and emit `/process/optical/boundary/setInvokeSD` when optical physics is enabled | DONE | Added `optical_boundary_invoke_sd` to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/boundary/setInvokeSD true/false` after `/run/initialize` when optical physics is enabled. Added `tests/test_optical_boundary_invoke_sd.py` with 6 tests covering round-trip, validation, project JSON persistence, true emission, false emission, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 28 focused tests pass; 773 broader regression tests pass (4 pre-existing unrelated failures). |
| G4CAP-025 | P1 | Physics | Add process inactivation support (`/process/inactivate`) to `EnvironmentState` and `generate_macro_file` | DONE | Added `process_inactivation` list to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, normalization). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/inactivate` commands after `/run/initialize`. Added `tests/test_process_inactivation.py` with 5 tests covering round-trip, validation, project JSON persistence, macro emission with/without particle, and omission when empty. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 28 focused tests pass; 774 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-026` as NEXT. |
| G4CAP-026 | P2 | Physics | Add EM process parameter support (`/process/em/applyCuts` and `/process/eLoss/fluct`) to `EnvironmentState` and `generate_macro_file` | DONE | Added `em_apply_cuts` (default `False`) and `eloss_fluct` (default `True`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/applyCuts true` when `em_apply_cuts` is `True` and `/process/eLoss/fluct false` when `eloss_fluct` is `False`, after `/run/initialize`. Added `tests/test_em_process_parameters.py` with 7 tests covering round-trip, validation, project JSON persistence, emission for each field individually, combined emission, and omission at defaults. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 7 new tests + 28 focused tests pass; 781 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-027` as NEXT. |
| G4CAP-027 | P2 | Fields | Add field propagation parameter support (`/field/stepperType` and `/field/setMinimumStep`) to `EnvironmentState` and `generate_macro_file` | DONE |
| G4CAP-028 | P2 | Optical Physics | Add `cerenkov_max_photons` to `EnvironmentState` and emit `/process/optical/cerenkov/setMaxPhotons` when optical physics is enabled | DONE | Added `cerenkov_max_photons` (default `0`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/cerenkov/setMaxPhotons {value}` after `/run/initialize` when optical physics is enabled and value is greater than zero. Added `tests/test_cerenkov_max_photons.py` with 6 tests covering round-trip, validation, project JSON persistence, positive emission, default omission, and optical-physics-off omission. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 6 new tests + 795 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-029` as NEXT. |
| G4CAP-029 | P2 | Optical Physics | Add `scintillation_by_particle_type` and `scintillation_finite_rise_time` to `EnvironmentState` and emit corresponding `/process/optical/scintillation/...` commands | DONE | Added `scintillation_by_particle_type` (default `False`) and `scintillation_finite_rise_time` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/scintillation/setByParticleType true` and `/process/optical/scintillation/setFiniteRiseTime true` after `/run/initialize` when optical physics is enabled and each flag is true. Added `tests/test_scintillation_process_controls.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 8 new tests + 191 broader regression tests pass. Marked `G4CAP-030` as NEXT. |
| G4CAP-030 | P2 | Run Control | Add `tracking_verbose` and `hits_verbose` to run manifest and emit `/tracking/verbose` and `/hits/verbose` before `/run/beamOn` | DONE | Added `tracking_verbose` (default `0`) and `hits_verbose` (default `0`) to `_default_scoring_run_manifest_defaults()` and `_normalize_scoring_run_manifest_defaults()` in `src/geometry_types.py`. Updated `generate_macro_file` in `src/project_manager.py` to emit `/tracking/verbose {value}` and `/hits/verbose {value}` before `/run/beamOn` when each value is greater than zero. Updated `static/scoringUi.js` to include the new fields in `SCORING_SAVED_RUN_CONTROL_KEYS`, `DEFAULT_RUN_MANIFEST_DEFAULTS`, and `normalizeRunManifestDefaults`. Added `tests/test_tracking_hits_verbose.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and ordering before `/run/beamOn`. Updated `tests/test_scoring_state.py` and `tests/js/scoring_ui.test.mjs` expected dicts for the new fields. All 8 new tests + 69 broader regression tests pass; JS tests pass. Backlog exhausted. |
| G4CAP-031 | P1 | Physics | Add `fluo` (atomic deexcitation) and `auger` boolean flags to `EnvironmentState` and emit `/process/em/fluo` and `/process/em/auger` in `generate_macro_file` | DONE | Added `fluo` (default `False`) and `auger` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/fluo true` and `/process/em/auger true` after `/run/initialize` when each flag is true. Added `tests/test_fluo_auger.py` with 7 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, and omission at defaults. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 7 new tests + 28 focused tests pass; 175 broader regression tests pass. Marked `G4CAP-032` as NEXT. |
| G4CAP-032 | P2 | Physics | Add `auger_cascade`, `pixe`, and `deexcitation_ignore_cut` boolean flags to `EnvironmentState` and emit corresponding `/process/em/...` commands in `generate_macro_file` | DONE | Added `auger_cascade`, `pixe`, and `deexcitation_ignore_cut` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/augerCascade true`, `/process/em/pixe true`, and `/process/em/deexcitationIgnoreCut true` after `/run/initialize` when each flag is true. Added `tests/test_auger_cascade_pixe_deexcitation.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, and omission at defaults. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 8 new tests + 28 focused tests pass; 826 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-033` as NEXT. |
| G4CAP-033 | P2 | Physics | Add `em_integral` boolean flag to `EnvironmentState` and emit `/process/em/integral` in `generate_macro_file` | DONE | Added `em_integral` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/integral true` after `/run/initialize` when the flag is true. Added `tests/test_em_integral.py` with 5 tests covering round-trip, validation, project JSON persistence, emission when true, and omission at default. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 22 focused tests pass; 197 broader regression tests pass. Backlog exhausted; performed discovery audit. |
| G4CAP-034 | P2 | Optical Physics | Add `cerenkov_track_secondaries_first` and `scintillation_track_secondaries_first` to `EnvironmentState` and emit corresponding `/process/optical/...` commands | DONE | Added `cerenkov_track_secondaries_first` (default `False`) and `scintillation_track_secondaries_first` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/cerenkov/setTrackSecondariesFirst true` and `/process/optical/scintillation/setTrackSecondariesFirst true` after `/run/initialize` when optical physics is enabled and each flag is true. Added `tests/test_cerenkov_scintillation_track_secondaries_first.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 8 new tests + 28 focused tests pass; 252 broader regression tests pass (4 pre-existing unrelated failures). |
| G4CAP-035 | P2 | Physics | Add `em_use_saturation` (Birks) boolean to `EnvironmentState` and emit `/process/em/UseG4EmSaturation` | DONE | Added `em_use_saturation` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/UseG4EmSaturation true` after `/run/initialize` when the flag is true. Added `tests/test_em_use_saturation.py` with 5 tests covering round-trip, validation, project JSON persistence, emission when true, and omission at default. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 28 focused tests pass; 844 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-036` as NEXT. |
| G4CAP-036 | P2 | Physics | Add `em_polarisation` boolean to `EnvironmentState` and emit `/process/em/Polarisation` | DONE | Added `em_polarisation` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/Polarisation true` after `/run/initialize` when the flag is true. Added `tests/test_em_polarisation.py` with 5 tests covering round-trip, validation, project JSON persistence, emission when true, and omission at default. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 177 broader regression tests pass (4 pre-existing unrelated failures). Backlog exhausted. |
| G4CAP-037 | P2 | Optical Physics | Add `cerenkov_stack_photons` and `scintillation_stack_photons` to `EnvironmentState` and emit `/process/optical/cerenkov/setStackPhotons` and `/process/optical/scintillation/setStackPhotons` | DONE | Added `cerenkov_stack_photons` (default `True`) and `scintillation_stack_photons` (default `True`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/cerenkov/setStackPhotons false` and `/process/optical/scintillation/setStackPhotons false` after `/run/initialize` when optical physics is enabled and each flag is `False`. Added `tests/test_optical_stack_photons.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 8 new tests + 857 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-038` as NEXT. |
| G4CAP-038 | P2 | Run Control | Add `run_verbose` and `event_verbose` to run manifest defaults and emit `/run/verbose` and `/event/verbose` before `/run/beamOn` | DONE | Added `run_verbose` (default `0`) and `event_verbose` (default `0`) to `_default_scoring_run_manifest_defaults()` and `_normalize_scoring_run_manifest_defaults()` in `src/geometry_types.py`. Updated `generate_macro_file` in `src/project_manager.py` to emit `/run/verbose {value}` and `/event/verbose {value}` before `/run/beamOn` when each value is greater than zero. Updated `static/scoringUi.js` to include the new fields in `SCORING_SAVED_RUN_CONTROL_KEYS`, `DEFAULT_RUN_MANIFEST_DEFAULTS`, and `normalizeRunManifestDefaults`. Added `tests/test_run_event_verbose.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and ordering before `/run/beamOn`. Updated `tests/test_scoring_state.py` and `tests/js/scoring_ui.test.mjs` expected dicts for the new fields. All 8 new tests + 191 broader regression tests pass. Marked `G4CAP-039` as NEXT. |
| G4CAP-039 | P2 | Physics | Add `em_verbose` to `EnvironmentState` and emit `/process/em/verbose` in `generate_macro_file` | DONE | Added `em_verbose` (default `0`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/verbose {value}` after `/run/initialize` when value is greater than zero. Added `tests/test_em_verbose.py` with 5 tests covering round-trip, validation, project JSON persistence, emission when positive, and omission at default. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 9 focused tests pass; 212 broader regression tests pass. Backlog exhausted. |

## Work Log

| Date | Task | Status | Summary |
| --- | --- | --- | --- |
| 2026-04-29 | Loop setup | DONE | Archived previous agent-loop docs, created continuous reliability context/tracker, and seeded the first backlog around offline UI dependencies plus Playwright-backed workflow validation. |
| 2026-04-29 | ACR-001 offline browser dependencies | DONE | Vendored Plotly, Marked, three.js, selected three.js addon modules, three-mesh-bvh, and three-bvh-csg under `static/vendor/`; switched `templates/index.html` to local static paths; updated README library notes. Next: ACR-002. |
| 2026-04-29 | ACR-002 startup baseline | DONE | Added `tests/test_startup_baseline.py` which opens AIRPET, creates a new geometry to reach the clean default state, verifies the hierarchy shows "World" and "box_PV (LV: box_LV)", checks the 3D canvas is visible, and asserts zero uncaught JS errors. Installed Playwright locally under `.local-packages/`; added `.local-packages/` to `.gitignore`; removed `/docs/` from `.gitignore` so tracker files can be committed. Test passes in 6.65s. Next: ACR-003. |
| 2026-04-29 | ACR-003 scoring smoke | DONE | Added `tests/test_scoring_smoke.py` which creates a new geometry, adds a GPS source, marks box_LV as a sensitive detector, adds a scoring mesh, runs a 10-event Geant4 simulation, and verifies the Analysis button becomes enabled with no uncaught JS errors. Test passes in ~10s. Also reinstalled greenlet in .local-packages/ for Python 3.10 compatibility. Next: ACR-004. |
| 2026-04-29 | ACR-004 save/load/reload | DONE | Added `tests/test_project_save_load_reload.py` which creates a new geometry, adds a GPS source, marks box_LV as sensitive, adds a scoring mesh, creates a rectangular drilled-hole generator on box_solid, exports the project JSON, reloads it via the hidden file input, and verifies that World, box_PV, the source, the scoring mesh, and the generator all persist with zero uncaught JS errors. Test passes in ~14s. Next: ACR-005. |
| 2026-04-29 | ACR-005 detector generator undo/reload | DONE | Added `tests/test_detector_generator_undo_reload.py` which creates a tiled sensor array generator on World, creates a channel cut array (boolean-cut) generator on box_solid, verifies both appear in the hierarchy, solids panel, and detector generators panel, undoes the channel cut creation, verifies stale boolean solids are cleaned up and box_solid is restored, reloads the page, and confirms the tiled sensor array persists with zero uncaught JS errors. Test passes in ~17s. Next: ACR-006. |
| 2026-04-29 | ACR-006 CAD import/reimport | DONE | Added `tests/test_cad_import_reimport.py` which creates a new geometry, verifies the CAD Imports accordion is hidden initially, imports `tests/fixtures/step/corpus/test_box.step`, verifies the panel appears with the import record and Reimport button, reimports `test_box_revised.step` via the panel reimport flow, verifies the record updates to the revised filename, and confirms other sidebar accordions remain functional with zero uncaught JS errors. Test passes in ~14s. Next: ACR-007. |
| 2026-04-29 | ACR-007 simulation warning surfacing | DONE | Updated `_build_simulation_log_payload` in `app.py` to treat stderr lines starting with `Warning:` as warnings (not errors), adding `has_warnings` to the log summary. Updated `pollSimStatus` in `static/main.js` to prefix `[WARNING]` instead of `[ERROR]` for warning lines. Added `test_simulation_status_api_distinguishes_warnings_from_errors` and `test_simulation_status_api_mixed_warnings_and_errors` to `tests/test_simulation_status_api.py`. All tests pass. Next: ACR-008. |
| 2026-04-29 | ACR-008 git ignore guidance | DONE | Updated `.gitignore` to ignore `.DS_Store`, `.playwright-mcp/`, root-level `test_*.yml` and `snapshot-*.md`, scratch GDML (`geant4/example_geometry.gdml`, `geant4/notes.txt`), `verification_run/`, and machine-local folders (`ref/`, `geom/`, `crysp/`). Verified `git status` no longer lists these artifacts. Next: ACR-009. |
| 2026-04-29 | ACR-008 git ignore guidance (test follow-up) | DONE | Added `tests/test_gitignore_hygiene.py` with 8 assertions covering Playwright artifacts, scratch GDML, verification output, machine-local folders, and `.DS_Store`. All tests pass. Fixed tracker `Current NEXT Task` header to ACR-009. Next: ACR-009. |
| 2026-04-29 | ACR-009 route-vs-AI parity | DONE | Added `tests/test_route_ai_parity_detector_generators.py` with two parity tests: `test_route_vs_ai_parity_tiled_sensor_array` and `test_route_vs_ai_parity_channel_cut_array`. Each test verifies that the direct Flask route (`/api/detector_feature_generators/upsert`) and the AI tool dispatch (`manage_detector_feature_generator`) produce equivalent geometry state. Comparison normalizes volatile object-ref `id` fields to focus on functional parity. All tests pass. Next: ACR-010. |
| 2026-04-29 | ACR-010 multimodal spike plan | DONE | Created `docs/AI_MULTIMODAL_SPIKE_PLAN.md` distilling `docs/AI_MULTIMODAL_ARTIFACT_INTAKE.md` into actionable acceptance criteria (artifact store routes, extraction→planning→execution, preflight cross-checks, parity reports, Playwright smoke) and enumerated fixture needs (upload files, extraction payloads, replay fixtures, UI sketch fixtures). Identified current test gaps: no artifact store route tests and no Playwright end-to-end coverage. No code changes. Backlog exhausted.
| 2026-04-29 | Self-expanding backlog setup | DONE | Added `docs/GEANT4_CAPABILITY_DISCOVERY_GUIDE.md`, updated the continuous-reliability context so an exhausted backlog triggers one bounded Geant4 capability-discovery audit, and seeded `G4DISC-001` as the next task. |
| 2026-04-29 | G4DISC-001 capability discovery | DONE | Inspected Geant4 `examples/extended/persistency/gdml/G04` (auxiliary SD info), `source/physics_lists/lists/src/` (~20 physics lists vs AIRPET's 5), and `examples/extended/runAndEvent/RE03` (command-based scoring meshes). Compared against AIRPET `src/gdml_writer.py`, `src/gdml_parser.py`, `templates/index.html`, `src/project_manager.py`, and `geant4/src/`. Found three concrete gaps and seeded `G4CAP-001` (GDML SD auxinfo), `G4CAP-002` (physics list expansion), and `G4CAP-003` (scoring mesh macro emission). Marked `G4CAP-001` as NEXT. |
| 2026-04-29 | G4CAP-001 GDML SD auxinfo round-trip | DONE | Updated `src/gdml_writer.py` to emit `<auxiliary auxtype="SensDet" auxvalue="{lv_name}"/>` inside `<volume>` for sensitive logical volumes. Updated `src/gdml_parser.py` to parse `<auxiliary auxtype="SensDet">` and set `lv.is_sensitive = True`. Added `test_gdml_sensitive_detector_round_trip` in `tests/test_gdml.py` verifying write, non-sensitive absence, and parse round-trip. Fixed duplicate auxiliary tag emission bug. All 31 tests in `tests/test_gdml.py` pass. Marked `G4CAP-002` as NEXT. |
| 2026-04-29 | G4CAP-002 physics list expansion | DONE | Added QBBC, QGSP_BERT_HP, QGSP_BIC, FTFP_BERT_ATL, and QGSP_FTFP_BERT to `#simPhysicsList` in `templates/index.html`. Added `tests/test_physics_list_options.py` with 3 tests covering acceptance of all 5 new lists, preservation of existing lists, and omission behavior. All tests pass. Marked `G4CAP-003` as NEXT. |
| 2026-04-29 | G4CAP-003 scoring mesh macro emission | DONE | Updated `src/project_manager.py` `generate_macro_file` to emit `/score/create/boxMesh`, `/score/mesh/boxSize`, `/score/mesh/translate/xyz`, `/score/mesh/nBin`, `/score/quantity/energyDeposit`, and `/score/close` for each enabled scoring mesh. Updated `geant4/main.cc` to instantiate `G4ScoringManager`. Added `tests/test_scoring_macro_emission.py` with 2 tests verifying enabled mesh emission and disabled mesh omission. C++ compiles and links successfully. Backlog exhausted. |
| 2026-04-29 | G4CAP-003 follow-up: quantity mapping + version-state fix | DONE | Fixed `generate_macro_file` hardcoded `energyDeposit` by mapping enabled tally requests to correct Geant4 `/score/quantity/` commands (cellFlux, doseDeposit, energyDeposit, nOfStep, nOfTrack, passageCellFlux, trackLength). Fixed `generate_macro_file` to use `temp_state` (saved version) instead of `self.current_geometry_state` for sensitive detectors and active sources, preventing simulations from using stale in-memory state. Added `test_generate_macro_emits_mapped_scoring_quantities_and_fallback` and `test_generate_macro_uses_version_state_not_current_state` to `tests/test_scoring_state.py`. All tests pass. |
| 2026-04-29 | G4DISC-002 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/examples/extended/runAndEvent/RE03` (command-based scoring meshes with cylindrical meshes and particle filters) and compared against AIRPET `src/project_manager.py`, `src/geometry_types.py`. Found two concrete gaps: (1) AIRPET only supports box scoring meshes; RE03 demonstrates `/score/create/cylinderMesh` with `/score/mesh/cylinderSize`. (2) AIRPET tally requests lack particle filters; RE03 demonstrates `/score/filter/particle gammaFilter gamma`. Seeded `G4CAP-004` (cylinder mesh support) and `G4CAP-005` (particle filter support). Marked `G4CAP-005` as NEXT. |
| 2026-04-30 | G4CAP-005 particle filter support | DONE | Added `particle_filter` dict (`filter_name`, `particle`) to `_normalize_scoring_tally_request_entry` in `src/geometry_types.py`; fixed `generate_macro_file` in `src/project_manager.py` to emit `/score/filter/particle {filter_name} {particle}` after the quantity command; propagated `particle_filter` through `build_scoring_runtime_plan` in `src/scoring_artifacts.py`. Updated existing test and added `test_generate_macro_emits_particle_filter_for_tally_requests` in `tests/test_scoring_macro_emission.py`. All 11 scoring tests + 54 broader regression tests pass. Marked `G4CAP-004` as NEXT. |
| 2026-04-30 | G4CAP-004 cylinder mesh support | DONE | Expanded `_SUPPORTED_SCORING_MESH_TYPES` to `{"box", "cylinder"}`; updated `_normalize_scoring_mesh_entry` to validate cylinder geometry (`rmin`, `rmax`, `z`) and bins (`r`, `phi`, `z`); updated `generate_macro_file` to emit `/score/create/cylinderMesh`, `/score/mesh/cylinderSize`, and cylinder `/score/mesh/nBin` commands. Added `test_generate_macro_emits_cylinder_mesh_commands`. Fixed test assertions for updated mesh_type error messages. All 12 scoring tests + 64 broader regression tests pass. Also committed a syntax-fix follow-up to `generate_macro_file` (duplicate `else` block from prior G4CAP-005 commit). Backlog exhausted; performed discovery audit. |
| 2026-04-30 | G4DISC-003 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/source/digits_hits/utils/src/G4ScoreQuantityMessenger.cc` and `ref/geant4-v11.3.2/source/digits_hits/utils/src/G4ScoringMessenger.cc` for supported scoring quantities, mesh types, and filters. Compared against AIRPET `src/geometry_types.py` and `src/project_manager.py`. Found three concrete gaps: (1) AIRPET only maps 7 tally quantities; Geant4 supports 15+ primitive scorers including `nOfSecondary`, `passageTrackLength`, `flatSurfaceCurrent`, `flatSurfaceFlux`, `nOfCollision`, `population`. (2) AIRPET only supports `box` and `cylinder` meshes; Geant4 supports `realWorldLogVol` scoring directly on logical volumes. (3) AIRPET only supports particle filters; Geant4 supports `charged`, `neutral`, and `kineticEnergy` filters. Seeded `G4CAP-006` (quantity expansion), `G4CAP-007` (realWorldLogVol meshes), and `G4CAP-008` (filter expansion). Marked `G4CAP-006` as NEXT. |
| 2026-04-30 | G4CAP-006 quantity expansion | DONE | Added 6 new tally quantities (`n_of_secondary`, `passage_track_length`, `flat_surface_current`, `flat_surface_flux`, `n_of_collision`, `population`) to `_SUPPORTED_SCORING_TALLY_QUANTITIES` in `src/geometry_types.py`, to `_G4_QUANTITY_MAP` in `src/project_manager.py`, and to `SUPPORTED_SCORING_TALLY_QUANTITIES` in `static/scoringUi.js`. Added `test_generate_macro_maps_new_quantities_to_g4_commands` in `tests/test_scoring_macro_emission.py` verifying each maps to the correct `/score/quantity/` command. Updated `tests/test_scoring_artifacts.py` and `tests/test_scoring_state.py` to reflect the expanded `supported_quantities` list. All 16 scoring tests pass. Marked `G4CAP-007` as NEXT. |
| 2026-04-30 | G4CAP-006 follow-up | DONE | Updated `tests/test_ai_api.py` `supported_quantities` assertions that were missed in the prior commit, preventing latent failures when pre-existing `KeyError`/`Subprocess` issues in those tests are resolved. Committed separately. |
| 2026-04-30 | G4CAP-007 realWorldLogVol mesh support | DONE | Expanded `_SUPPORTED_SCORING_MESH_TYPES` to include `realWorldLogVol`; updated `_normalize_scoring_mesh_entry` to require `geometry.logical_volume_name` and optional `geometry.copy_number_level`; updated `generate_macro_file` to emit `/score/create/realWorldLogVol {lv_name} {copy_level}` and skip mesh geometry commands. Added `test_generate_macro_emits_real_world_log_vol_commands` in `tests/test_scoring_macro_emission.py`. Updated mesh-type error-message assertions in `tests/test_scoring_state.py`. All 17 scoring tests pass. Marked `G4CAP-008` as NEXT. |
| 2026-04-30 | G4CAP-008 charged/neutral/kineticEnergy filters | DONE | Added `charged_filter`, `neutral_filter`, and `kinetic_energy_filter` normalization to `_normalize_scoring_tally_request_entry` in `src/geometry_types.py`; updated `generate_macro_file` in `src/project_manager.py` to emit `/score/filter/charged`, `/score/filter/neutral`, and `/score/filter/kineticEnergy` commands; propagated new filters through `build_scoring_runtime_plan` in `src/scoring_artifacts.py`; updated `normalizeScoringTally` in `static/scoringUi.js` to preserve all filter fields. Added `test_generate_macro_emits_charged_neutral_and_kinetic_energy_filters` in `tests/test_scoring_macro_emission.py`. All 18 scoring tests + 64 broader regression tests pass. Backlog exhausted; performed discovery audit. |
| 2026-04-30 | G4DISC-004 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/source/digits_hits/utils/src/G4ScoringMessenger.cc` and `ref/geant4-v11.3.2/source/digits_hits/utils/src/G4ScoreQuantityMessenger.cc` for supported scoring mesh types, quantities, and filters. Compared against AIRPET `src/geometry_types.py` and `src/project_manager.py`. Found three concrete gaps: (1) Geant4 supports `/score/create/probe pname halfSize unit checkOverlap`; AIRPET does not support probe meshes. (2) Geant4 supports `/score/filter/particleWithKineticEnergy fname Elow Ehigh unit p0 .. pn`; AIRPET supports particle and kineticEnergy filters separately but not combined. (3) Geant4 supports 18+ primitive scorers including `cellCharge`, `passageCellCurrent`, `volumeFlux`, `nOfTerminatedTrack`, `minKinEAtGeneration`; AIRPET maps 13 quantities and omits these 5. Seeded `G4CAP-009` (probe mesh support), `G4CAP-010` (particleWithKineticEnergy filter), and `G4CAP-011` (quantity expansion). Marked `G4CAP-009` as NEXT. |
| 2026-04-30 | G4CAP-009 probe mesh support | DONE | Expanded `_SUPPORTED_SCORING_MESH_TYPES` to include `probe`; updated `_normalize_scoring_mesh_entry` in `src/geometry_types.py` to validate `geometry.half_size_mm`, `geometry.unit`, and `geometry.check_overlap`; updated `generate_macro_file` in `src/project_manager.py` to emit `/score/create/probe {name} {halfSize} {unit} {checkOverlap}` and skip mesh geometry commands for probe meshes. Updated `normalizeScoringMesh` in `static/scoringUi.js` to preserve incoming `mesh_type` (fixing a bug where all loaded meshes were forced to `box`) and handle probe geometry. Added `test_generate_macro_emits_probe_mesh_commands` in `tests/test_scoring_macro_emission.py`. Updated mesh-type error-message assertions in `tests/test_scoring_state.py`. All 19 scoring tests pass. Marked `G4CAP-010` as NEXT. |
| 2026-04-30 | G4CAP-010 particleWithKineticEnergy filter | DONE | Added `particle_with_kinetic_energy_filter` normalization to `_normalize_scoring_tally_request_entry` in `src/geometry_types.py`; updated `generate_macro_file` in `src/project_manager.py` to emit `/score/filter/particleWithKineticEnergy {filter_name} {e_low} {e_high} {unit} {particles}`; propagated new filter through `build_scoring_runtime_plan` in `src/scoring_artifacts.py`; updated `normalizeScoringTally` in `static/scoringUi.js` to preserve the combined filter. Added `test_generate_macro_emits_particle_with_kinetic_energy_filter` in `tests/test_scoring_macro_emission.py`. All 20 scoring tests pass. Marked `G4CAP-011` as NEXT. |
| 2026-04-30 | G4CAP-011 missing primitive scorers | DONE | Added `cell_charge`, `passage_cell_current`, `volume_flux`, `n_of_terminated_track`, and `min_kin_e_at_generation` to `_SUPPORTED_SCORING_TALLY_QUANTITIES` in `src/geometry_types.py`, `_G4_QUANTITY_MAP` in `src/project_manager.py`, and `SUPPORTED_SCORING_TALLY_QUANTITIES` in `static/scoringUi.js`. Added `test_generate_macro_maps_additional_quantities_to_g4_commands` in `tests/test_scoring_macro_emission.py`. Updated `tests/test_scoring_artifacts.py`, `tests/test_scoring_state.py`, and `tests/test_ai_api.py` with expanded `supported_quantities`. All 22 scoring tests pass. Backlog exhausted. |
| 2026-04-30 | G4CAP-011 follow-up | DONE | Overlapping automation run detected duplicate test `test_generate_macro_maps_final_five_quantities_to_g4_commands` in working tree; removed it, leaving the original `test_generate_macro_maps_additional_quantities_to_g4_commands`. All 21 scoring tests pass. |
| 2026-04-30 | G4DISC-005 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/examples/extended/optical/OpNovice/gdml/NoviceExample.gdml` and compared against AIRPET `src/geometry_types.py`, `src/gdml_parser.py`, `src/gdml_writer.py`, and `geant4/main.cc`. Found three concrete gaps: (1) AIRPET `Material` class has no `properties` dict and the GDML parser ignores `<property>` tags under `<material>`, so optical properties like RINDEX and ABSLENGTH are lost on import/export. (2) AIRPET has no API/UI for editing material optical properties. (3) The C++ runtime registers `G4OpticalPhysics` but never creates `G4MaterialPropertiesTable` from project state. Seeded `G4CAP-012` (GDML material property round-trip), `G4CAP-013` (material optical properties API), and `G4CAP-014` (C++ runtime material properties table). Marked `G4CAP-012` as NEXT. |
| 2026-04-30 | G4CAP-012 GDML material properties round-trip | DONE | Added `properties` dict to `Material` class in `src/geometry_types.py` (init, to_dict, from_dict). Updated `src/gdml_parser.py` `_parse_materials` to read `<property name="..." ref="..."/>` under `<material>`. Updated `src/gdml_writer.py` `_add_materials` to emit `<property>` tags for each material property. Added `test_gdml_material_properties_round_trip` in `tests/test_gdml.py` verifying write, containment, and parse round-trip. All 32 GDML tests + 21 scoring tests pass. Marked `G4CAP-013` as NEXT. |
| 2026-04-30 | G4CAP-013 backend API for material optical properties | DONE | Verified that `ProjectManager.add_material` and `update_material` already pass `properties` through kwargs after G4CAP-012. Added `tests/test_material_properties_api.py` with 4 focused tests covering add-material with properties, update-material properties, and `Material.from_dict` round-trip for both empty and populated properties dicts. All 36 GDML + material tests pass. Marked `G4CAP-014` as NEXT. |
| 2026-04-30 | G4CAP-014 C++ runtime material properties table | DONE | Added `/g4pet/detector/addMaterialConstProperty` C++ messenger command; `DetectorConstruction::ConstructSDandField()` iterates over `fMaterialConstProperties`, looks up materials in `G4MaterialTable`, creates `G4MaterialPropertiesTable`, and calls `AddConstProperty`. `generate_macro_file` emits the command for numeric material properties. `gdml_writer.py` skips numeric properties (they are handled by macro) and only emits string references when the referenced matrix exists in defines. Removed duplicate test in `tests/test_gdml.py`. Fixed the surviving GDML test to include matrix defines so `<property>` tags are emitted. Added `tests/test_material_properties_runtime.py` with 3 tests covering numeric property emission, string ref omission, and mixed properties. All 72 tests pass; C++ compiles and links. Marked `G4CAP-015` as NEXT. |
| 2026-04-30 | G4DISC-006 capability discovery | DONE | Inspected `geant4/main.cc` (physics list and optical physics registration), `app.py` `get_geant4_env` (env var forwarding), `geant4/src/DetectorConstruction.cc` (user limits but no step limiter process), and `ref/geant4-v11.3.2/source/processes/biasing/generic/` (biasing operators). Compared against AIRPET runtime and physics setup. Found three concrete gaps: (1) `G4StepLimiterPhysics` is never registered, so region user limits are not enforced. (2) Energy-dependent material optical properties (vectors) are not supported beyond GDML string references. (3) Optical physics enablement is not persisted in project state. Seeded `G4CAP-015`, `G4CAP-016`, and `G4CAP-017`. Marked `G4CAP-015` as NEXT. |
| 2026-04-30 | G4CAP-015 step limiter physics | DONE | Added `#include "G4StepLimiterPhysics.hh"` and `physicsList->RegisterPhysics(new G4StepLimiterPhysics())` in `geant4/main.cc` so region user limits are enforced. Added `tests/test_step_limiter_physics.py` with a batch-mode macro test that asserts `Registering G4StepLimiterPhysics` appears in the executable output. C++ compiles and links; all 72 regression tests pass. Marked `G4CAP-016` as NEXT. |
| 2026-04-30 | G4CAP-016 energy-dependent material optical properties | DONE | Added backend support for property vectors: GDML parser resolves `coldim="2"` matrices into lists, writer emits them as synthetic matrix defines, macro generator emits `/g4pet/detector/addMaterialProperty`, and C++ calls `AddProperty`. Files changed: `src/gdml_parser.py`, `src/gdml_writer.py`, `src/project_manager.py`, `geant4/include/DetectorConstruction.hh`, `geant4/src/DetectorConstruction.cc`, `tests/test_gdml.py`, `tests/test_material_properties_runtime.py`. All 43 focused tests pass; C++ compiles and links. Marked `G4CAP-017` as NEXT. |
| 2026-04-30 | G4CAP-017 optical physics persistence | DONE | Added `optical_physics` to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate). Updated `static/scoringUi.js` `buildResolvedSimulationOptions` to fallback to `projectState.environment.optical_physics` before transient default. Updated `buildSimulationOptionOverrides` to compare against the same fallback. Removed hardcoded `optical_physics: false` from `AppState.simOptions` in `static/main.js` so loaded project state is respected. Updated `UIManager.setSimOptions` in `static/uiManager.js` to set `simPhysicsListSelect` and `simOpticalPhysicsCheckbox`. Added `tests/test_optical_physics_persistence.py` with 7 tests covering round-trip, validation, project JSON save/load, and `get_geant4_env` forwarding. Updated `tests/test_environment_state.py` for the new field. All 69 regression tests pass. Backlog exhausted. |
| 2026-04-30 | G4CAP-017 follow-up | DONE | Fixed latent assertion failure in `tests/test_ai_api.py::test_ai_tool_update_property_and_get_component_details_cover_environment_field` caused by the new `optical_physics` field in `EnvironmentState.to_dict()`. Added `"optical_physics": False` to the expected dict. All 66 focused regression tests pass. Backlog exhausted. |
| 2026-05-01 | G4DISC-007 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/examples/extended/radioactivedecay/rdecay01` (ion gun macros and `PrimaryGeneratorAction.cc`) and `rdecay02/run.mac`. Compared against AIRPET `src/project_manager.py` and `geant4/main.cc`. Found three concrete gaps: (1) AIRPET only supports GPS sources and cannot emit `/gun/particle ion` or `/gun/ion` macros needed for radioactive decay. (2) `geant4/main.cc` does not register `G4RadioactiveDecayPhysics`, so decay chains are not simulated. (3) AIRPET UI and AI tools have no ion source parameter fields (Z, A, Q, E). Seeded `G4CAP-018` (ion source backend), `G4CAP-019` (radioactive decay physics registration), and `G4CAP-020` (ion source UI/AI). Marked `G4CAP-018` as NEXT. |
| 2026-05-04 | G4CAP-018 ion source backend | DONE | Extended `ParticleSource` in `src/geometry_types.py` with `source_type` (`gps`/`ion`) and `ion_params` dict (`Z`, `A`, `Q`, `excitation_energy_keV`). Updated `to_dict`/`from_dict` for round-trip persistence. Updated `add_particle_source` and `add_source` in `src/project_manager.py` to accept and forward the new fields. Updated `generate_macro_file` to emit `/gps/particle ion` and `/gps/ion {Z} {A} {Q} {E}` for ion sources, skipping duplicate `particle`/`ion` gps_commands keys. Added `tests/test_ion_source_macro.py` with 4 tests: ion command emission, spurious-gps-command skipping, GPS source regression, and dict round-trip. All 134 focused tests pass. Marked `G4CAP-019` as NEXT. |
| 2026-05-04 | G4CAP-019 radioactive decay physics | DONE | Added `#include "G4RadioactiveDecayPhysics.hh"` and conditional registration in `geant4/main.cc` based on `G4RADIOACTIVEDECAYPHYSICS` env var. Updated `app.py` `get_geant4_env` to forward `radioactive_decay_physics` from `sim_params`. Added `tests/test_radioactive_decay_physics.py` with 5 tests: registration when env var set, no registration when absent, and `get_geant4_env` true/false/omission coverage. C++ compiles and links; all 5 new tests + 33 broader regression tests pass. Marked `G4CAP-020` as NEXT. |
| 2026-05-04 | G4CAP-020 ion source UI/AI | DONE | Added source type selector and ion param inputs (Z, A, Q, excitation energy) to the GPS editor modal in `templates/index.html`. Updated `static/gpsEditor.js` to toggle GPS vs Ion controls, collect ion params, and pass `source_type`/`ion_params` in the callback. Updated `static/apiService.js`, `static/main.js`, `app.py` routes, and `src/project_manager.py` to accept and persist the new fields. Updated `static/uiManager.js` inspector to display ion params. Added AI tool aliases and defaults for `source_type` and `ion_params` in `app.py`, and updated the `manage_particle_source` handler to forward them. Also fixed a pre-existing G4CAP-017 `main.js` corruption that broke the entire frontend module graph (large chunk accidentally deleted). Restored `main.js` from the parent commit and re-applied the correct `optical_physics` removal. Added `tests/test_ion_source_ui.py` (Playwright) verifying creation, inspector display, and round-trip persistence. Added `tests/test_ai_api.py::test_ai_tool_manage_particle_source_ion_params` verifying AI tool create/update with ion params. All 25 focused regression tests pass; Playwright test passes in ~8.6s. Backlog exhausted.
| 2026-05-04 | G4DISC-008 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/examples/extended/optical/OpNovice/src/OpNoviceDetectorConstruction.cc` (optical surface, skin surface, and border surface creation) and compared against AIRPET `src/geometry_types.py`, `src/gdml_parser.py`, `src/gdml_writer.py`, `geant4/src/DetectorConstruction.cc`, and `src/project_manager.py`. Found two concrete gaps: (1) AIRPET parses and writes `<opticalsurface>`, `<skinsurface>`, and `<bordersurface>` in GDML, but has no round-trip tests for them. (2) The C++ runtime never creates `G4OpticalSurface`, `G4LogicalSkinSurface`, or `G4LogicalBorderSurface`, and `generate_macro_file` emits no macro commands for them, so optical surfaces defined in the UI/GDML are silently ignored at simulation runtime. Seeded `G4CAP-021` (GDML round-trip tests) and `G4CAP-022` (C++ runtime support and macro emission). Marked `G4CAP-021` as NEXT. |
| 2026-05-07 | G4CAP-021 | BLOCKED | Blocker persists: 14 tracked files with uncommitted changes (874 insertions, 45 deletions). Changes include unrelated AI backend adapter work (`app.py`, `src/ai_backend_adapters.py`, `src/ai_tools.py`, `static/aiAssistant.js`, `static/aiRuntimeConfigUi.js`, `static/apiService.js`, `templates/index.html`, and AI test files) alongside partial optical-surfaces work (`src/gdml_parser.py`, `src/geometry_types.py`, `tests/test_gdml.py`). Cannot safely mix unrelated AI changes with G4CAP-021 reliability work. Human intervention needed to commit, stash, or discard the unrelated tracked changes before optical-surfaces tasks can proceed. |
| 2026-05-07 | G4CAP-021 | BLOCKED | Re-confirmed on automation run: same 14 tracked files, same unrelated AI adapter changes mixed with partial optical-surfaces work. Blocker unchanged. No safe work item available. Stopping. |
| 2026-05-08 | G4CAP-021 | BLOCKED | Blocker persists: 14 tracked files with uncommitted changes outside docs (same files as 2026-05-07). Changes remain the same unrelated AI backend adapter work mixed with partial optical-surfaces changes. Cannot safely proceed with G4CAP-021 or G4CAP-022 until the working tree is cleaned. Stopping. |
| 2026-05-08 | G4CAP-021 | DONE | Working tree is now clean (no tracked changes outside docs). Existing optical-surface, skin-surface, and border-surface round-trip tests in `tests/test_gdml.py` were already present. Only `test_gdml_border_surface_round_trip` failed because it asserted imported `physvol1_ref`/`physvol2_ref` equal the original Python object UUIDs, which is impossible after XML round-trip. Fixed test to verify the imported IDs resolve to PVs named `box_pv`. All 35 GDML tests pass. Marked `G4CAP-022` as NEXT. |
| 2026-05-10 | G4CAP-022 | DONE | Added `/g4pet/detector/addOpticalSurface`, `addSkinSurface`, `addBorderSurface` messenger commands to `geant4/src/DetectorConstruction.cc`; updated `generate_macro_file` in `src/project_manager.py` to emit them with PV-name resolution for border surfaces; added enum parsing helpers for `G4SurfaceType`, `G4OpticalSurfaceModel`, and `G4OpticalSurfaceFinish`. Added `tests/test_optical_surface_macro.py` (2 tests) and `tests/test_optical_surface_cpp.py` (1 test). C++ compiles and links; all 3 new tests + 27 focused regression tests pass. Backlog exhausted. |
| 2026-05-10 | G4DISC-009 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/examples/extended/optical/OpNovice/src/OpNoviceDetectorConstruction.cc` (optical surface property table attachment) and compared against AIRPET `src/geometry_types.py`, `src/gdml_parser.py`, `src/gdml_writer.py`, `geant4/src/DetectorConstruction.cc`, `src/project_manager.py`, and `static/opticalSurfaceEditor.js`. Found one concrete gap: AIRPET's `OpticalSurface` class has a `properties` dict, the GDML parser/writer round-trips `<property>` tags under `<opticalsurface>`, the UI allows editing properties as matrix-define refs, and the AI tool forwards them. However, the C++ runtime never creates a `G4MaterialPropertiesTable` for optical surfaces or calls `G4OpticalSurface::SetMaterialPropertiesTable`, so energy-dependent properties like REFLECTIVITY and EFFICIENCY are silently ignored at simulation runtime. Seeded `G4CAP-023` (C++ runtime optical surface properties). Marked `G4CAP-023` as NEXT. |
| 2026-05-10 | G4CAP-023 optical surface properties runtime | DONE | Added C++ messenger commands and macro emission for optical surface const/vector properties; updated `ConstructSDandField` to attach `G4MaterialPropertiesTable` to optical surfaces. Updated GDML writer to handle list properties. Added 4 new tests. All 67 focused tests pass. C++ compiles and links. Backlog exhausted. |
| 2026-05-10 | G4DISC-010 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4OpticalParametersMessenger.cc` for configurable optical process parameters and compared against AIRPET `geant4/main.cc`, `src/geometry_types.py`, and `src/project_manager.py`. Found one concrete gap: Geant4 supports `/process/optical/boundary/setInvokeSD` (critical for optical photon detection at boundaries), `/process/optical/cerenkov/setMaxPhotons`, and other optical process controls, but AIRPET's `EnvironmentState` only has an `optical_physics` on/off flag and `generate_macro_file` never emits `/process/optical/...` commands. Seeded `G4CAP-024` (`optical_boundary_invoke_sd` persistence and macro emission). Marked `G4CAP-024` as NEXT. |
| 2026-05-11 | G4CAP-024 optical boundary invoke SD | DONE | Added `optical_boundary_invoke_sd` to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/boundary/setInvokeSD true/false` after `/run/initialize` when optical physics is enabled. Added `tests/test_optical_boundary_invoke_sd.py` with 6 tests covering round-trip, validation, project JSON persistence, true emission, false emission, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 28 focused tests pass; 773 broader regression tests pass (4 pre-existing unrelated failures). Backlog exhausted. |
| 2026-05-11 | G4DISC-011 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/source/processes/management/src/G4ProcessTableMessenger.cc` (process activation/inactivation), `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4EmParametersMessenger.cc` (EM process parameters), and `ref/geant4-v11.3.2/examples/extended/field/` (field stepper/minimum-step controls). Compared against AIRPET `src/geometry_types.py`, `src/project_manager.py`, `geant4/src/DetectorConstruction.cc`, and `geant4/main.cc`. Found three concrete gaps: (1) AIRPET has no data model or macro emission for `/process/inactivate` or `/process/activate` commands. (2) AIRPET has no data model for EM process toggles like `/process/em/applyCuts` or `/process/eLoss/fluct`. (3) AIRPET only sets field vector values but never controls the integration stepper or minimum step size. Seeded `G4CAP-025` (process inactivation), `G4CAP-026` (EM process parameters), and `G4CAP-027` (field propagation parameters). Marked `G4CAP-025` as NEXT. |
| 2026-05-11 | G4CAP-025 process inactivation | DONE | Added `process_inactivation` list to `EnvironmentState` with normalization, validation, round-trip persistence, and macro emission. Updated `generate_macro_file` to emit `/process/inactivate` commands after `/run/initialize`. Added `tests/test_process_inactivation.py` (5 tests). Updated existing test dict assertions for the new field. All 5 new tests + 28 focused tests pass; 774 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-026` as NEXT. |
| 2026-05-11 | G4CAP-026 EM process parameters | DONE | Added `em_apply_cuts` (default `False`) and `eloss_fluct` (default `True`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/applyCuts true` when `em_apply_cuts` is `True` and `/process/eLoss/fluct false` when `eloss_fluct` is `False`, after `/run/initialize`. Added `tests/test_em_process_parameters.py` with 7 tests covering round-trip, validation, project JSON persistence, emission for each field individually, combined emission, and omission at defaults. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 7 new tests + 28 focused tests pass; 781 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-027` as NEXT. |
| 2026-05-11 | G4CAP-027 field propagation parameters | DONE | Added `field_stepper_type` (default `""`) and `field_minimum_step_mm` (default `0.0`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/field/stepperType {type}` when non-empty and `/field/setMinimumStep {value} mm` when greater than zero, after field assignments and before region controls. Added `tests/test_field_propagation_parameters.py` with 7 tests covering round-trip, validation, project JSON persistence, stepper-only emission, minimum-step-only emission, combined emission, and omission at defaults. Updated `tests/test_environment_state.py`, `tests/test_geant4_field_macro.py`, `tests/test_ai_api.py`, and `tests/test_field_aware_examples.py` for the new fields in expected dicts. All 7 new tests + 28 focused tests pass; 788 broader regression tests pass (4 pre-existing unrelated failures). Backlog exhausted. |
| 2026-05-11 | G4DISC-012 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4OpticalParametersMessenger.cc` (optical process parameters: `/process/optical/cerenkov/setMaxPhotons`, `/process/optical/scintillation/setByParticleType`, `/process/optical/scintillation/setFiniteRiseTime`), `ref/geant4-v11.3.2/source/tracking/src/G4TrackingMessenger.cc` (`/tracking/verbose`), and `ref/geant4-v11.3.2/source/digits_hits/detector/src/G4SDmessenger.cc` (`/hits/verbose`). Compared against AIRPET `src/geometry_types.py`, `src/project_manager.py`, `geant4/main.cc`, and `static/scoringUi.js`. Found three concrete gaps: (1) AIRPET has no data model for Cerenkov process controls like `setMaxPhotons`. (2) AIRPET has no data model for scintillation process controls like `setByParticleType` and `setFiniteRiseTime`. (3) AIRPET's run manifest lacks tracking/hits verbosity controls (`/tracking/verbose`, `/hits/verbose`) despite commented-out code in `generate_macro_file`. Seeded `G4CAP-028` (Cerenkov max photons), `G4CAP-029` (scintillation process controls), and `G4CAP-030` (tracking/hits verbosity). Marked `G4CAP-028` as NEXT. |
| 2026-05-12 | G4CAP-027 follow-up bug fix | DONE | Fixed runtime failure: `/field/stepperType` and `/field/setMinimumStep` commands were emitted in the macro but failed with "COMMAND NOT FOUND" because `G4FieldBuilder` (and its `G4FieldParametersMessenger`) was only created inside `ConstructSDandField()`, which runs during `/run/initialize`. Moved `G4FieldBuilder::Instance()` and global parameter creation into `DetectorConstruction::DetectorConstruction()` so the messengers exist before macro execution. Also removed `SetStepperType(kClassicalRK4)` from `ConfigureElectroMagneticFieldParameters()` to respect user-configured stepper types. Changed `ConstructSDandField()` to call `fieldBuilder->ConstructFieldSetup()` unconditionally (matching Geant4 field examples), preventing a segfault during cleanup. Added `test_field_stepper_type_and_minimum_step_accepted_at_runtime` to `tests/test_geant4_field_smoke.py` verifying the C++ runtime accepts both commands and the simulation completes. All 4 field smoke tests + 32 focused regression tests pass; 788 broader regression tests pass (4 pre-existing unrelated failures). |
| 2026-05-20 | G4CAP-028 Cerenkov max photons | DONE | Added `cerenkov_max_photons` (default `0`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/cerenkov/setMaxPhotons {value}` after `/run/initialize` when optical physics is enabled and value is greater than zero. Added `tests/test_cerenkov_max_photons.py` with 6 tests covering round-trip, validation, project JSON persistence, positive emission, default omission, and optical-physics-off omission. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 6 new tests + 795 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-029` as NEXT. |
| 2026-05-20 | G4CAP-029 scintillation process controls | DONE | Added `scintillation_by_particle_type` (default `False`) and `scintillation_finite_rise_time` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/scintillation/setByParticleType true` and `/process/optical/scintillation/setFiniteRiseTime true` after `/run/initialize` when optical physics is enabled and each flag is true. Added `tests/test_scintillation_process_controls.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 8 new tests + 191 broader regression tests pass. Marked `G4CAP-030` as NEXT. |
| 2026-05-20 | G4CAP-030 tracking/hits verbosity | DONE | Added `tracking_verbose` (default `0`) and `hits_verbose` (default `0`) to `_default_scoring_run_manifest_defaults()` and `_normalize_scoring_run_manifest_defaults()` in `src/geometry_types.py`. Updated `generate_macro_file` in `src/project_manager.py` to emit `/tracking/verbose {value}` and `/hits/verbose {value}` before `/run/beamOn` when each value is greater than zero. Updated `static/scoringUi.js` to include the new fields in `SCORING_SAVED_RUN_CONTROL_KEYS`, `DEFAULT_RUN_MANIFEST_DEFAULTS`, and `normalizeRunManifestDefaults`. Added `tests/test_tracking_hits_verbose.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and ordering before `/run/beamOn`. Updated `tests/test_scoring_state.py` and `tests/js/scoring_ui.test.mjs` expected dicts for the new fields. All 8 new tests + 69 broader regression tests pass; JS tests pass. Backlog exhausted. |
| 2026-05-20 | G4DISC-013 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4EmLowEParametersMessenger.cc` (atomic deexcitation: `/process/em/fluo`, `/process/em/auger`, `/process/em/augerCascade`, `/process/em/pixe`, `/process/em/deexcitationIgnoreCut`) and `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4EmParametersMessenger.cc` (integral method: `/process/em/integral`). Compared against AIRPET `src/geometry_types.py`, `src/project_manager.py`, and `geant4/main.cc`. Found three concrete gaps: (1) AIRPET has no data model or macro emission for atomic deexcitation (`fluo`) or Auger electron production (`auger`). (2) AIRPET has no support for Auger cascade, PIXE, or deexcitation cut override. (3) AIRPET has no control over the EM integral method for secondary generation. Seeded `G4CAP-031` (`fluo` and `auger`), `G4CAP-032` (`auger_cascade`, `pixe`, `deexcitation_ignore_cut`), and `G4CAP-033` (`em_integral`). Marked `G4CAP-031` as NEXT. |
| 2026-05-20 | G4CAP-031 fluo and auger | DONE | Added `fluo` (default `False`) and `auger` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/fluo true` and `/process/em/auger true` after `/run/initialize` when each flag is true. Added `tests/test_fluo_auger.py` with 7 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, and omission at defaults. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 7 new tests + 28 focused tests pass; 175 broader regression tests pass. Marked `G4CAP-032` as NEXT. |
| 2026-05-20 | G4CAP-032 auger cascade, PIXE, deexcitation ignore cut | DONE | Added `auger_cascade`, `pixe`, and `deexcitation_ignore_cut` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/augerCascade true`, `/process/em/pixe true`, and `/process/em/deexcitationIgnoreCut true` after `/run/initialize` when each flag is true. Added `tests/test_auger_cascade_pixe_deexcitation.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, and omission at defaults. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 8 new tests + 28 focused tests pass; 826 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-033` as NEXT. |
| 2026-05-20 | G4CAP-033 em_integral | DONE | Added `em_integral` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/integral true` after `/run/initialize` when the flag is true. Added `tests/test_em_integral.py` with 5 tests covering round-trip, validation, project JSON persistence, emission when true, and omission at default. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 22 focused tests pass; 197 broader regression tests pass. Backlog exhausted. |
| 2026-05-20 | G4DISC-014 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4OpticalParametersMessenger.cc` (remaining optical process parameters after G4CAP-024/028/029: `/process/optical/cerenkov/setTrackSecondariesFirst`, `/process/optical/scintillation/setTrackSecondariesFirst`, `/process/optical/cerenkov/setStackPhotons`, `/process/optical/scintillation/setStackPhotons`) and `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4EmParametersMessenger.cc` (EM process toggles: `/process/em/UseG4EmSaturation`, `/process/em/Polarisation`). Compared against AIRPET `src/geometry_types.py`, `src/project_manager.py`, and `geant4/main.cc`. Found three concrete gaps: (1) AIRPET has no data model or macro emission for Cerenkov/scintillation `setTrackSecondariesFirst`, which controls whether optical secondaries are tracked before the parent. (2) AIRPET has no control over built-in Birks saturation (`UseG4EmSaturation`) for scintillation quenching. (3) AIRPET has no control over gamma polarisation in EM processes. Seeded `G4CAP-034` (`cerenkov_track_secondaries_first` and `scintillation_track_secondaries_first`), `G4CAP-035` (`em_use_saturation`), and `G4CAP-036` (`em_polarisation`). Marked `G4CAP-034` as NEXT. |
| 2026-05-20 | G4CAP-034 track secondaries first | DONE | Added `cerenkov_track_secondaries_first` (default `False`) and `scintillation_track_secondaries_first` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/cerenkov/setTrackSecondariesFirst true` and `/process/optical/scintillation/setTrackSecondariesFirst true` after `/run/initialize` when optical physics is enabled and each flag is true. Added `tests/test_cerenkov_scintillation_track_secondaries_first.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 8 new tests + 28 focused tests pass; 252 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-035` as NEXT. |
| 2026-05-20 | G4CAP-035 em_use_saturation (Birks) | DONE | Added `em_use_saturation` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/UseG4EmSaturation true` after `/run/initialize` when the flag is true. Added `tests/test_em_use_saturation.py` with 5 tests covering round-trip, validation, project JSON persistence, emission when true, and omission at default. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 28 focused tests pass; 844 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-036` as NEXT. |
| 2026-05-21 | G4CAP-036 em_polarisation | DONE | Added `em_polarisation` (default `False`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/Polarisation true` after `/run/initialize` when the flag is true. Added `tests/test_em_polarisation.py` with 5 tests covering round-trip, validation, project JSON persistence, emission when true, and omission at default. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 177 broader regression tests pass (4 pre-existing unrelated failures). Backlog exhausted. |
| 2026-05-21 | G4DISC-015 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4OpticalParametersMessenger.cc` for remaining optical process parameters after G4CAP-034/035/036. Found `setStackPhotons` commands for Cerenkov and scintillation (controls whether optical photons are put on the tracking stack). Inspected `ref/geant4-v11.3.2/source/run/src/G4RunMessenger.cc` and `source/event/src/G4EvManMessenger.cc` for run/event verbosity (`/run/verbose`, `/event/verbose`). Inspected `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4EmParametersMessenger.cc` for EM verbosity (`/process/em/verbose`). Seeded three evidence-backed tasks: G4CAP-037 (optical stack photons), G4CAP-038 (run/event verbosity), G4CAP-039 (EM verbosity). Marked `G4CAP-037` as NEXT. |
| 2026-05-21 | G4CAP-037 optical stack photons | DONE | Added `cerenkov_stack_photons` (default `True`) and `scintillation_stack_photons` (default `True`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/cerenkov/setStackPhotons false` and `/process/optical/scintillation/setStackPhotons false` after `/run/initialize` when optical physics is enabled and each flag is `False`. Added `tests/test_optical_stack_photons.py` with 8 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, and omission when optical physics is off. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 8 new tests + 857 broader regression tests pass (4 pre-existing unrelated failures). Marked `G4CAP-038` as NEXT. |
| 2026-05-21 | G4CAP-038 run/event verbosity | DONE | Added `run_verbose` and `event_verbose` to run manifest defaults and emit `/run/verbose` and `/event/verbose` before `/run/beamOn`. All 8 new tests + 191 broader regression tests pass. Marked `G4CAP-039` as NEXT. |
| 2026-05-21 | G4CAP-039 EM verbosity | DONE | Added `em_verbose` (default `0`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/em/verbose {value}` after `/run/initialize` when value is greater than zero. Added `tests/test_em_verbose.py` with 5 tests covering round-trip, validation, project JSON persistence, emission when positive, and omission at default. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new field in expected dicts. All 5 new tests + 9 focused tests pass; 212 broader regression tests pass. Backlog exhausted. |
| 2026-05-21 | G4DISC-016 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4OpticalParametersMessenger.cc` for remaining optical process parameters after G4CAP-034/035/036/037/038/039. Found three concrete gaps: (1) AIRPET has no data model for `/process/optical/cerenkov/setMaxBetaChange` (Cerenkov max beta change threshold). (2) AIRPET has no data model for `/process/optical/scintillation/setTrackInfo` (scintillation track information). (3) AIRPET has no data model for `/process/optical/wls/setTimeProfile` or `/process/optical/wls2/setTimeProfile` (WLS/WLS2 time profiles). Seeded `G4CAP-040` (Cerenkov max beta change), `G4CAP-041` (scintillation track info), and `G4CAP-042` (WLS/WLS2 time profiles). Marked `G4CAP-040` as NEXT. |
| 2026-05-21 | G4CAP-042 WLS/WLS2 time profiles | DONE | Added `wls_time_profile` (default `""`) and `wls2_time_profile` (default `""`) to `EnvironmentState` in `src/geometry_types.py` (init, to_dict, from_dict, validate, to_summary_dict). Updated `generate_macro_file` in `src/project_manager.py` to emit `/process/optical/wls/setTimeProfile {value}` and `/process/optical/wls2/setTimeProfile {value}` after `/run/initialize` when optical physics is enabled and each value is non-empty. Added `tests/test_wls_time_profile.py` with 9 tests covering round-trip, validation, project JSON persistence, individual emission, combined emission, omission at defaults, omission when optical physics is off, and summary dict behavior. Updated `tests/test_environment_state.py`, `tests/test_ai_api.py`, `tests/test_field_aware_examples.py`, and `tests/test_geant4_field_macro.py` for the new fields in expected dicts. All 9 new tests + 302 broader regression tests pass. Backlog exhausted. |
| 2026-05-21 | G4DISC-017 capability discovery | DONE | Inspected `ref/geant4-v11.3.2/source/processes/electromagnetic/utils/src/G4EmParametersMessenger.cc` for remaining EM process parameters after G4CAP-026/031/032/033/035/036/039. Compared against AIRPET `src/geometry_types.py`, `src/project_manager.py`, and `geant4/main.cc`. Found three concrete gaps: (1) AIRPET has no data model for `/process/eLoss/LPM` (Landau-Pomeranchuk-Migdal effect toggle, default true). (2) AIRPET has no data model for `/process/msc/LateralDisplacement` or `/process/msc/MuHadLateralDisplacement` (MSC lateral displacement sampling, defaults true). (3) AIRPET has no data model for `/process/msc/StepLimit` or `/process/msc/StepLimitMuHad` (MSC step limitation type, default "UseSafety"). Seeded `G4CAP-043` (LPM effect), `G4CAP-044` (MSC lateral displacement), and `G4CAP-045` (MSC step limit). Marked `G4CAP-043` as NEXT. |
